# TB710FU ColorOS 16 内核兼容性与后续移植说明

## 结论

可以在本地编译新内核，也可以先靠外挂模块补一部分一加特性，但两条路线的边界很明确：

1. **现在就适合外挂**：缺失的稳定 ABI、只依赖 GKI 导出符号的 vendor hook、诊断节点。例如 Horae 的 `/proc/shell-temp`。
2. **可以改写后外挂并 A/B 测试**：`zram_opt`、`kswapd_opt` 的独立子功能。必须移除与 Lenovo/Qualcomm `mem-hooks.ko` 重复注册的 restricted hook，并按 8GB/12GB 实机压力重新定参数。
3. **必须成套替换或重编**：HybridSwap、OPlus zram/zsmalloc、swapd/kcompressd，以及依赖其内部结构的模块。它们不是标准 zram 上方的薄插件。
4. **不应原样移植**：OPD2513 的 SM8850 调度、频域、热区和设备树策略。目标 CPU 是六核 SM8650Q，真实容量是 `1+4+1`，而源系统平台、核数和热传感器均不同。

因此推荐先构建一个可卸载的 `oplus_compat` 外挂层，消除确定的用户空间重试和接口缺口；等拿到 Lenovo vendor kernel/DT 全量源码与可恢复刷写链路后，再做 GKI + vendor modules 的完整集成。直接把一加预编译 `.ko` 放进当前系统不可行。

### 风驰/HMBIRD 的边界

OPD2513 分支中的“风驰”实现不是可独立加载到任意 GKI 的 governor。公开源码可见的
核心链是 `sched_ext/HMBIRD II + sched_assist + frame_boost + UAG/频率控制 +
game_opt`：HMBIRD 代码直接访问 `struct rq`、`struct task_struct.scx`、
`ext_sched_class`、SCX dispatch queue 和 OPlus UX task 扩展，并要求内核提供
`CONFIG_SCHED_CLASS_EXT` 及一组 `android_vh_scx_*` hook。

当前 build 13606743 的 `.config` 没有 `CONFIG_SCHED_CLASS_EXT`，源码中也没有
`include/linux/sched/ext.h`，ABI 列表不存在 `ext_sched_class/scx` 符号。因此：

- HMBIRD/风驰主体**不能作为外挂 KO 加入当前内核**，也不能靠伪造 proc 节点实现；
- 只开启 BPF/JIT 没有用，因为当前内核没有 sched_ext 调度类；
- 若要真正加入，需要把 Qualcomm/OPlus 对 sched_ext、HMBIRD、WALT、sched_assist、
  frame_boost 和 UAG 的同代修改合入内核并重编 Image 与受影响 vendor modules；
- `task_struct`/`rq` 布局变化会改变 KMI CRC，不能在仍加载 Lenovo 原 vendor KO 的
  情况下只替换一个 GKI Image；这应作为取得 Lenovo vendor 全源码后的独立内核分支。

当前可安全做的是保留 Lenovo Qualcomm WALT，并将风驰的“按场景识别关键线程、
短时 boost、帧期频率响应”思想按六核 `1+4+1` 重新实现到独立 Scene 模块；这属于
行为兼容，不宣称已经移植风驰内核。游戏识别与 frame hint 必须拿真实 ColorOS
生产者日志验证后逐项接入，不能照搬 SM8850 的 CPU mask、capacity 或频率表。

## 1. 已确认的两套基线

### 1.1 当前平板内核

- 设备：Lenovo TB710FU，平台 `pineapple`，六核 SM8650Q。
- 内核：`6.1.128-android14-11-g5c2cea985a84-ab13606743`。
- Google GKI 构建：`13606743`。
- GKI 分支：`aosp_kernel-common-android14-6.1-2025-03`。
- common 精确提交：`5c2cea985a841939e6d074cbed2019dec0245fcd`。
- 关键配置：`CONFIG_MODVERSIONS=y`、`CONFIG_TRIM_UNUSED_KSYMS=y`、`CONFIG_ANDROID_VENDOR_HOOKS=y`、标准 `zram.ko`/`zsmalloc.ko`、MGLRU、memcg、PSI。
- CPU 容量：CPU0=379，CPU1-4=867，CPU5=1024，即 `1+4+1`；cpufreq 因硬件域拆成 policy0/1/3/5（`1+2+2+1`）。

Google CI 仍提供该构建的 `modules_prepare_outdir.tar.gz`、`kernel_aarch64_Module.symvers`、`vmlinux`、`Image`、头文件与 ABI 列表，因此可以做真正精确的外部模块构建，而不是只按“6.1 大版本相同”碰运气。

### 1.2 ColorOS 用户空间来源

- ROM 身份：`OPD2513 / OP6547L1`。
- OPD2513 是一加平板 3 Pro，原平台为 SM8850，而不是 SM8650。
- 正确的官方模块参考分支是：
  `OnePlusOSS/android_kernel_modules_and_devicetree_oneplus_sm8850`
  的 `oneplus/sm8850_b_16.0_pad_3_pro`，当前核对提交为
  `8be53e8b737a83a33512d5e0106cccb010a5c24c`。

SM8650 一加源码仍可用于理解较老的 OPlus 接口演进，但不能再当作 OPD2513 的同版本实现。当前问题的本质是 **SM8850 ColorOS 用户空间运行在 Lenovo SM8650Q/6.1 vendor kernel 上**。

## 2. 已观察到的不兼容

| 用户空间预期 | 当前 Lenovo 内核 | 影响 | 建议 |
| --- | --- | --- | --- |
| `/proc/shell-temp` | 缺失 | Horae 每约5秒 `open` 失败，形成无意义重试；OPlus 壳温聚合链断开 | 外挂恢复原厂 ABI，低风险 |
| `/proc/oplus_mem/dynamic_swappiness` | 缺失 | OSense/内存策略无法控制 OPlus 动态 swappiness | 先关闭对应用户空间动态策略；后续只移植不冲突的 hook |
| `/proc/oplus_mem/alloc_adjust_ctrl`、`kswapd_load_stat` | 缺失 | OPlus 高阶分配与 kswapd 诊断/控制链不完整 | 可选外挂，必须先做压力 A/B |
| zram `hybridswap_*` sysfs | 标准 zram 无这些属性 | HybridSwap 初始化为空，用户空间仍声明 nandswap/hybridswap 能力 | 当前禁用 OSense dynaZram/cMem；完整替换时再开启 |
| OPlus sched_assist/frame_boost/UAG/game/eas proc 栈 | 全部缺失 | 源系统调度提示不能按原平台落地 | 保留六核 Scene 独立调度，不伪造空节点 |
| OPD2513 热区与 DT | Lenovo 传感器命名/硬件不同 | Horae、thermal HAL 及源机策略可能读空或读到幽灵热区 | 仅映射真实 Lenovo 传感器；DT 级功能等待源码合并 |

当前已加载的 Qualcomm `mem-hooks.ko` 会注册
`android_rvh_set_balance_anon_file_reclaim` 等 hook。OPlus `zram_opt` 也会注册其中一部分；restricted vendor hook 通常不允许两个实现同时接管，原样加载会失败或形成策略冲突。

首次重启还确认 vendor init 会在 post-fs-data 之后把
`vm.min_free_kbytes` 从65536覆盖回11584。修复模块现利用已有的四分钟有界
启动窗口复校该值；这是一次性启动竞态处理，不新增常驻守护。

## 3. 已落地的外挂验证

仓库的 `kernel-compat/oplus_compat/oplus_shell_temp_compat.c` 实现了 OPD2513 官方 Horae 使用的协议：

- 写：`<index> <milli-celsius>`，index 0/1/2 分别保存三路 Horae 壳温估算；
- 读：返回三路中的最大值；
- 未收到数据前返回0，与官方实现的初始行为一致；
- 不注册不存在的 OPlus DT 热区，不伪造固定温度；
- 卸载模块会删除节点，完整可回滚。

本地已成功生成 ARM64 `oplus_shell_temp_compat.ko`：

- vermagic：`6.1.128-android14-11-g5c2cea985a84-ab13606743 SMP preempt mod_unload modversions aarch64`；
- 所有18个外部符号 CRC 均逐项匹配 build 13606743 的官方 `Module.symvers`；
- 真机手动加载成功，节点自动获得 `u:object_r:horae_proc:s0` 标签；Horae 每5秒写入的真实值由0更新到32500，加载后不再出现 `open /proc/shell-temp failed`。KernelSU allowlist/feature_config 哈希前后不变。现已并入 FixModule，由 post-fs-data 一次性 `insmod`，不增加守护进程。

第二个可安全编译的模块是 `oplus_mm_compat.ko`，它把 `zram_opt` 与
`kswapd_opt` 中只依赖公开 GKI hook、且不和 Qualcomm `mem-hooks.ko` 冲突的部分
重新实现为一个模块：

- 提供原格式 `swappiness_para`、`dynamic_swappiness`，但只下调、不抬高内核或
  memcg 已选择的 swappiness；kswapd/direct reclaim 安全天花板分别为50/20；
- 提供真实 `kswapd_debug`、`kswapd_load_stat` 慢路径/唤醒/耗时统计；两组
  诊断 hook 默认完全不注册，仅在控制节点写1时存在，避免常态原子计数开销；
- `alloc_adjust_ctrl` 默认关闭且 hook 不注册；实机捕获到频繁 order-9 慢路径，
  不应屏蔽其回收，否则可能把显示/GPU压力变成直接分配失败；
- `compat_status` 明确返回 `standard_zram`、`hybridswap=0`，不欺骗 OSense。

本地 `W=1` 构建结果：vermagic 与 build 13606743 完全相同，37个导入符号 CRC
全部匹配；连续二次构建 SHA-256 一致。真机手动加载已验证 proc 标签、限幅、
统计和卸载路径，KernelSU allowlist 哈希保持不变；仍需用最终低开销版本完成重启
与长待机测试后才加入开机自动加载。

压力现场还确认标准 zram 在约3～4GB逻辑占用时会出现 `zsmalloc` order-0
分配失败：当前 GKI 的 zram 明确在两条 `zs_malloc` 路径追加
`__GFP_MOVABLE | __GFP_CMA`，CMA耗尽后会在换出路径嵌套 direct reclaim，并与
显示恢复争抢。外挂兼容层现以仅作用于导出符号 `zs_malloc(pool,size,gfp)` 的
kprobe清除CMA标志，不改变普通页、DMA、显示或相机分配。真机同条件测试命中
143,227次后CMA由0恢复到31MB，换入/换出分别下降约60%/71%，kswapd/direct
扫描下降约53%/71%。进一步40/50/60交叉测试后保留64MB水位和swappiness=50；完整内核移植时应把同一
修改直接合入 `zram_drv.c`，以消除kprobe过渡层。

本轮同时审计了其它看似“能编”的 OPlus KO：完整 HybridSwap 与 HMBIRD 缺少内核
主体；IO metrics/task cpustats 主要增加采样开销；SM8850 UAG、frame_boost、
sched_assist 依赖私有结构和错误的核簇拓扑。它们不满足“能安全加入并提高本机性能”
的条件，故没有为了数量强行产出不可加载或负优化的 KO。

构建命令：

```bash
bash kernel-compat/tools/build_gki_compat.sh
```

脚本只下载公开源码/构建制品并编译，不刷写、不加载模块。

## 4. 外挂模块可实现到什么程度

### A级：适合立即外挂

- Horae `/proc/shell-temp` ABI；
- 只读诊断和统计接口；
- 用户空间确实存在生产者/消费者、语义可由官方源码确认的简单 proc/sysfs 兼容层；
- 当前 GKI 已导出所需符号的非侵入 vendor hook。

原则是恢复真实数据链，不创建“永远返回成功/固定值”的假接口。假接口只会让 ColorOS 误判能力存在，可能引发更深的循环或错误策略。

### B级：改写后可外挂

`zram_opt` 可拆成动态 swappiness、anon/file reclaim 和额外水位三部分。当前 GKI 有
`android_vh_tune_swappiness` 与部分 vmscan hooks，但：

- `android_rvh_set_balance_anon_file_reclaim` 已由 Qualcomm `mem-hooks.ko` 使用，兼容模块不能重复注册；
- 当前 GKI 似乎没有 OPD2513 版本依赖的全部水位 hook；
- OPD2513 `zram_opt` 还依赖其 zram、sched_assist 与 mm_osvelte，不能原文件直接编译；
- 一加默认 kswapd swappiness 偏高是为完整 HybridSwap/zram 控制面设计，不能直接套到当前标准 zram。

`kswapd_opt` 的高阶分配 `__GFP_RECLAIM` 调整和负载统计理论上可拆出，但关闭 costly-order reclaim 可能把卡顿转成分配失败，必须以 SurfaceFlinger、相机、WebView、多任务和长待机为测试集，不能只看一次跑分。

### C级：必须成套替换

OPD2513 的 `oplus_bsp_hybridswap_zram` 包含自己的 `zram_drv.c`、zcomp、HybridSwap core、hybridswapd、kcompressd 和压缩实现，并与 OPlus mm_osvelte/sched_assist 等模块联动。它与当前已加载的标准 `zram.ko` 互斥，不能在系统启动后并排外挂。

可行的完整方案是：

1. 以当前 6.1 GKI/KMI 为基线，把 OPD2513 代码向后移植到6.1 API；
2. 同时构建匹配的 zram、zsmalloc/压缩及所有直接依赖；
3. 在 early boot 替换 `system_dlkm`/`vendor_dlkm` 中对应模块与加载顺序；
4. 在启用 OSense dynaZram/cMem 前验证所有 proc/sysfs 控制节点与 SELinux 标签；
5. 任何一步失败都必须能从另一 slot 或 fastboot/救砖包恢复。

这属于内核发行版改造，不应由 KernelSU 的普通 `service.sh` 在已运行系统里完成。

## 5. 推荐的新内核结构

### 阶段一：保持原 GKI Image，外挂兼容层

- 使用 build 13606743 精确 headers、generated out 和 Module.symvers；
- 每个兼容点独立 `.ko`，默认不自动加载；
- 真机手动 `insmod`，验证24小时和一次长待机后再加入 early-init；
- 不改 KernelSU 授权数据库、不覆盖 KSU 模块目录。

这是当前最优路线，能最快修掉确定的接口缺口，且不会同时改变调度、回收、温控和启动链。

### 阶段二：重编 GKI 6.1，但保持现有 vendor ABI

- common 固定在 `5c2cea985a841939e6d074cbed2019dec0245fcd`；
- 使用 CI `.config`、KMI symbol list 和尽量相同的 Android Clang 17；
- 不删除 Lenovo vendor 模块所需导出符号，不改变页面大小和关键内核配置；
- 优先把稳定的小兼容层做 built-in 或受控模块；
- 继续沿用 Lenovo DTBO、vendor_boot、vendor_dlkm，仅替换经过验证的 Image/boot。

该阶段可以产出“新 GKI 内核”，但并不等于已经拥有完整 OPlus 内核栈。

### 阶段三：融合 OPlus MM/调度能力

- MM 顺序：诊断接口 → zram_opt 子集 → kswapd_opt 子集 → 完整 zram/HybridSwap；
- CPU 顺序：保留 Qualcomm WALT/Power HAL → 只移植缺失 hint 接口 → 最后才评估 sched_assist/frame_boost；
- Thermal 顺序：先建立 Lenovo 真实传感器映射表，再修改 DT/thermal zone，禁止导入 SM8850 阈值表；
- 每加入一组功能都重新跑 ABI、模块加载、启动、首次解锁、长待机和温控测试。

## 6. 还需要的 Lenovo 源码与制品

当前 Google GKI 公共源码已经精确齐全；真正缺的是 Lenovo vendor 部分。后续完整融合至少需要：

- TB710FU 对应版本的 vendor kernel modules 源码；
- DTS/DTBO、defconfig fragments、module lists/blocklists；
- Qualcomm/Lenovo 的 build config、Module.symvers 或完整 Bazel/Kleaf 工程；
- 当前可正常启动的 boot、vendor_boot、dtbo、vendor_dlkm、system_dlkm 备份；
- 已验证的 fastboot/EDL/双 slot 回滚方式。

如果之前提供的 Lenovo 下载方式能重新放进仓库文档或脚本，应把源码提交/压缩包哈希固定下来；仅有“最新版”下载地址无法保证日后可复现当前内核。

## 7. 真机验收清单

每个内核或外挂版本至少执行：

1. KMI：vermagic、全部 modversion CRC、未解析符号、模块签名策略；
2. 启动：冷启动到首次解锁，记录 CPU/memory/io PSI、vmstat、`system_server`/SF/kswapd；
3. 图形：后台切换、最近任务、多窗口、144Hz、长待机亮屏；
4. 内存：8GB 与12GB，0～1×RAM zram，低/中/高压力，检查 swap 往返和 direct reclaim；
5. 温控：空闲、充电、持续负载，确认 CPU 可 boost 且真实过热仍降频；
6. 外设：手写笔连接/断开/首吸附/振动、蓝牙耳机、杜比与空间音频；
7. 安全：KernelSU 授权列表哈希前后不变，LSPosed 与模块挂载不丢失；
8. 长稳：至少一次过夜待机后复测，不以刚开机5分钟结果作为结论。

## 8. 明确禁止的捷径

- 不加载来自另一构建号、另一 Android 内核或 SM8850 的预编译 `.ko`；
- 不在标准 zram 已启用后再加载 OPlus zram/HybridSwap；
- 不同时注册 Qualcomm `mem-hooks` 已占用的 restricted hook；
- 不照搬 SM8850 的 CPU mask、频率、thermal zone 和 DT；
- 不用常驻 shell 每秒改 sysctl/cpuset 来模拟缺失内核逻辑；
- 不伪造固定温度、固定“成功”状态或空 proc 节点欺骗用户空间；
- 未验证回滚前不写当前唯一可启动 slot。

## 9. 上游资料

- [Google Android common kernel](https://android.googlesource.com/kernel/common/)
- [Google GKI CI 下载脚本](https://android.googlesource.com/kernel/build/+/refs/heads/main-kernel-build-2023/gki/download_from_ci)
- [当前 GKI build 13606743 制品页](https://ci.android.com/builds/submitted/13606743/kernel_aarch64/latest/)
- [一加官方 OPD2513/SM8850 模块与设备树仓库](https://github.com/OnePlusOSS/android_kernel_modules_and_devicetree_oneplus_sm8850/tree/oneplus/sm8850_b_16.0_pad_3_pro)
- [一加官方 SM8650 模块与设备树仓库](https://github.com/OnePlusOSS/android_kernel_modules_and_devicetree_oneplus_sm8650)
- [Linux cgroup v1 memory controller 文档](https://github.com/torvalds/linux/blob/master/Documentation/admin-guide/cgroup-v1/memory.rst)
