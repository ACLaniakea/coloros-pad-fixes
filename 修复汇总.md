# ColorOS 移植修复汇总（ACLaniakea 2.0.0）

设备：联想小新 Pro GT（TB710FU / SM8650Q / pineapple），ColorOS 16 (OPD2513) 移植，KernelSU + LSPosed。
版本：活跃合并模块 `coloros_port_fix` 为 `2.0.0`（versionCode 2000）；`base-fix/module` 与 `port-tuning` 仅保留为历史参考。作者 ACLaniakea，包名 `com.aclaniakea.*`。

协议：本项目采用 [MIT License](LICENSE)，仅供个人学习与设备移植研究使用。

## 一、基础修复（base-fix）

### KernelSU 模块 `coloros_port_base_fix`

| 修复项 | 实际作用 | 实现位置 |
| --- | --- | --- |
| AON QNN 生命周期 | 把原厂 AIBoost/QNN(HTP) 运行时 bind 进 AON 应用链接器命名空间，启动命名空间加载器，恢复真实 NPU 推理生命周期；规避移植栈 nativeDelete 崩溃 | post-fs-data.sh / aon-namespace-loader.sh / zygisk |
| AON QNN ODM 配置 | 补齐移植 ROM 缺失的 /odm/etc/camera QNN 配置 | post-fs-data.sh |
| 环境光自适应 | 恢复 oplus.product.display_features.xml，色温自适应重新生效 | post-fs-data.sh |
| 144Hz 刷新率 | 覆盖云端 60/90/120 策略，提供 TB710FU 144Hz 策略 | post-fs-data.sh |
| 显示色域映射 | 生动=原生色域(256)、自然=sRGB(0)，修正移植 ROM 色温标签偏暗 | post-fs-data.sh / system.prop |
| 录音增益 | HAL 级绑定 speaker-mic 采集增益 TX_DEC 96 / ADC 16，修复小布唤醒与录音音量偏小/破音 | post-fs-data.sh / mixer_paths |
| Tango 32 位兼容 | 停止不兼容 tango zygote，bind 翻译过的 32 位 libdl | post-fs-data.sh |
| 性能 HAL 兼容 | 停用 source-device perf HAL，开机恢复 CPU 频点硬件上限；后续热保护仍由原厂 thermal-engine/Thermal HAL 接管，不使用轮询拉频守护 | service.sh |
| thermal 重载 | 模块挂载后重载 thermal-engine，CPU 策略生效 | service.sh |
| 小布 BWV 唤醒 | 启用/解冻 OVoice 与 SpeechAssist、写入全局唤醒开关与唤醒词、安装原厂模型、OVMS 检测窗口延长至 6000ms | service.sh |
| 电池健康入口 | 开启电池健康页开关，数据由 LSPosed 桥接真实 sysfs | service.sh |
| 序列号补齐 | ro.serialno 等空属性从真实属性继承 | common.sh |
| 应用建议修复 | 修正“应用建议”卡片协议配置 | service.sh / card-protocol-patcher.jar |

### LSPosed Hook `com.aclaniakea.colorosostatsguard`

- AonYuvLayoutBridge：归一化前置摄像头 YUV_420_888 平面布局，抑制移植 nativeDelete 崩溃，保持真实推理结果；
- AmbientColorSensorBridge：环境光色温自适应桥接（CCT 输出与 eyeprotect_display_cct 对齐）；
- ColorOSVoiceWakeupBridge：小布 BWV 唤醒链路（PCM 增益、阈值缩放、非流式完整模型、VPR 声纹 speakerId 保留、唤醒窗口与重挂时序）；
- BatteryHealthBridge / CpuHealthInfoBridge / RealtimeGpuBridge：电池健康、CPU/GPU 信息桥接；
- OStatsCpuGuard：避免 OStats 在 CPU 频点数组不匹配时反复格式化超长错误日志；当真实 `skin-msm-therm` 已低于 43°C 时清除移植 Thermal HAL 错误滞留的 Severe(3)，保留 46.5°C 虚拟 skin 触发线与原厂内核/电池/充电保护；
- SoundTriggerTrackerGuard / LenovoPenBridgeGuard / LenovoColorModeBridge / OcrScannerCameraBridge / CellularIdentityUiGuard / OplusSystemIdentityBridge：对应系统兼容性钩子。

## 二、调优（port-tuning）

### KernelSU 模块 `coloros_port_tuning`

- post-fs-data 阶段 bind mount 覆盖 `/my_stock/etc/extension/` 下三份 osense 配置：关闭实测会在应用切换时触发大量压缩与换入的主动 swap 策略；IO/MEM PSI 配置同时移除交互期间的高频清理规则；
- 开机阶段根与 apps 父组 `swappiness=5`，压低 zygote 进程迁组前的换出；稳定态普通组=10、冷后台=20、活跃组与 system_server=5，避免 0/1 极端值并减少交互换页；
- 保留 ROM 既有 ZRAM（兼容 8/12GB RAM 机型的 0～1×RAM 配置，不创建或改容量），仅在真实内存压力下由内核按需使用；NAND swap 保持关闭，不新增常驻调优守护；
- `vm.min_free_kbytes` 提升到 64MB，给内核留足水位余量，避免 direct-reclaim / allocstall 停顿；
- 参数在开机阶段一次性写入，延迟创建的 system_server memcg 由有界等待补写后立即退出；action 按钮仅打印诊断。

## 三、手写笔桥接（pen-bridge）

### KernelSU 模块 `lenovo_pen_bridge`

- 开机按已绑定地址直接调用原厂 CoreService `CONNECT_PENCIL` BLE 连接（不以 Hall/CPS 为前提）；
- 设置页断开调用 `DISCONNECT_PENCIL`，由 Hook 执行真实 `BluetoothGatt.disconnect()`，设备/HID 真实离开连接态；
- 常驻监控真实 ACL/GATT/Hall/CPS 事件，驱动设置页卡片/磁吸胶囊/电量/存在状态（只接受真实事件）；
- CPS 开机上电：仅真实磁吸 Hall 0:1 时保持 GPIO；磁吸只负责充电与弹窗；
- PenHidCtl（priv-app，纯服务无图标）HID 连接控制，只调后台 PenHidService；
- TB710FU 刷新率策略 bind mount；
- 屏幕唤醒后 1.2s 延迟状态回放，`pen_wakeup_*` 节点只在开机按需写一次，避免黑屏。

### priv-app `com.aclaniakea.penhidctl`（PenHidCtl）

- 纯服务应用（仅 PenHidService，无 Activity），**桌面无图标**；
- 以 priv-app 形式随模块挂载到 `/system/priv-app/aclpenhid`，持有 BLUETOOTH_PRIVILEGED / WRITE_SECURE_SETTINGS 权限；
- HID 连接/断开由 Root 服务通过 `am startservice` 调后台服务完成，无启动器入口。

### LSPosed Hook `com.aclaniakea.lenovopenbridge`

- BLE/GATT、System Server、IPeManager、Hall、设备空间与设置逻辑桥接；
- 设备空间手写笔存在键只跟随真实蓝牙连接；电量未知保留最后一次有效电量；
- 不涉及亮度/背光/屏幕电源参数。

## 四、更新日志

### 2.0.0（合并修复模块）

- 修复 1.2.x 合并回归：此前 apps 父分组及应用子 memcg 实际仍为 `swappiness=100`；实机 A/B 压测后采用开机根/apps=5，稳定后普通=10、冷后台=20、活跃与 system_server=5，并关闭会放大交互换页的 OSense 主动换出；
- 修复 systemserver cgroup 延迟创建、错过 service 一次性写入的问题：加入 1 秒低频、命中即退出、最长 120 秒的有界修正，不增加常驻调优守护；
- `vm.min_free_kbytes` 32MB→64MB，在 8GB 设备上提前唤醒 kswapd，减少 UI 分配进入 direct reclaim/allocstall；
- AON namespace loader 从每 50ms `pidof`（20Hz）改为阻塞监听 ActivityManager `am_proc_start` 事件；实测旧实现 4 小时消耗约 7.5 分钟 CPU/52 万次调度，新实现空闲 20 秒 CPU 与调度计数均零，同时可在 AON 新 PID 上完成真实 QNN runtime bind；
- 把 apps 父 memcg 与 64MB `min_free_kbytes` 前移到 post-fs-data 首段，并同步修正高通开机脚本的全部水位写入，避免开机前一分钟仍以 ROM 的 5–11MB 水位发生 direct reclaim；
- 自动恢复被精简策略禁用的原厂 `com.oplus.romupdate`，修复 Scene“关闭无障碍服务”开关的 `Could not find provider`，不覆盖用户自身开关值；
- 冷启动显式启用并 unstop 外部 Dolby Bridge 包/服务组件，再配置 idle 与 appops 白名单；修复重装或重启后包处于 stopped 状态、原厂设置页能显示但隐式 Binder 服务无法启动的问题；
- 杜比 Bridge 同步 `system_dolby` 与外层 `system_effect_profile`，避免 DAX 已启用而音效页仍显示“原始”；场景、GEQ 与 10 段自定义值均由 Settings 持久化并在服务重建时重新写入 DAX；
- 手写笔模块普通广播进程不再硬依赖 LSPosed 日志类，修复 Hall/电池广播偶发进程崩溃导致控制中心设备卡片刷新不稳定；
- 手写笔振动的 OEM GATT 就绪状态绑定当前 IPeManager PID，开机清理上次会话标记；原厂进程/会话不存在时自动回退直连 GATT，并对特征值/描述符写入失败进行有界重试，避免重启后命令落入失效广播而无振动；
- 修复 QTI Thermal HAL 虚拟 skin 与设备物理皮温偏差过大、降温后仍长期保持 Severe(3) 的移植问题：system_server 仅在真实 `skin-msm-therm` ≤43°C 时清除陈旧状态，保留 46.5°C Severe 触发线及全部底层 thermal-engine/内核保护，不增加常驻拉频服务；
- action 诊断补充 apps 根 memcg 与 CPU/内存/I/O PSI，卸载时恢复 ROM memcg=100 和默认 11584KB 空闲水位；
- 现场 4 应用驻留后 3 轮切换对比主动换出方案：swap-in 约减少 75%、swap-out 约减少 64%、direct scan 约减少 95%，第 3 轮启动耗时为 244/254/161/170ms；首次同时装入大型应用仍会发生真实内存压力，未通过危险温控放宽掩盖。

### 1.1.5（调优模块）

- `coloros_port_tuning` 1.1.4 → 1.1.5 / versionCode 1105；
- 定位关键机制：本内核全局回收不遵守 per-memcg swappiness（cgroup v1 无内存限制时按全局值从全局 LRU 换出），per-memcg 10 无法阻止关键进程被换出；
- 全局 `vm.swappiness` 40→20（实测关键进程换出立即停止增长并回落），post-fs-data 阶段即生效。

### 1.1.4（调优模块）

- `coloros_port_tuning` 1.1.3 → 1.1.4 / versionCode 1104；
- 定位剩余卡顿：保护在 service 阶段（boot_completed+8s）才生效，开机早期以 ROM 默认 swappiness=100 已把关键进程换出约 570MB；
- post-fs-data 阶段尽早降全局 swappiness 并写关键 memcg，杜绝开机早期换出窗口；
- 追加禁用 `sys_osense_memory_decisionmaker_config.xml` 的 `reentrant/memstop`、`zram`、`mem_leak` 清理规则（此前 MEM PSI 仍触发 reentrant）；
- 移除模块内从未生效的 `my_stock/` 死配置。

### 1.1.3（调优模块）

- `coloros_port_tuning` 1.1.2 → 1.1.3 / versionCode 1103；
- 实测定位剩余卡顿来源：即使关键进程防换出生效，系统仍持续约 120MB/s 换出、76MB/s 换入（10s 内 3.1 万页换出），zram 抖动是动画拖慢/掉帧主因；
- post-fs-data bind mount 禁用 osense 后台应用主动 zram 换出（swap `switch=false`）并移除 IO PSI `reentrant/iostop` 高频清理；
- 关键 memcg swappiness 20→10、后台 60→40、`min_free_kbytes` 32MB→64MB，进一步压缩换页抖动。

### 1.1.2（调优模块）

- `coloros_port_tuning` 1.1.1 → 1.1.2 / versionCode 1102；
- 实测定位长待机唤醒卡顿根因：zram 中 system_server/launcher/systemui 被换出约 800MB，点亮时全部 swap-in 造成动画拖慢掉帧；
- `active`/`systemserver` memcg `swappiness` 20（防换出），后台 apps 保持 60；`min_free_kbytes` 提升到 32MB 防 direct-reclaim 停顿；
- 新增常驻守护每 30s 复校关键参数，防 osense 回改。

### 1.1.1（调优模块）

- `coloros_port_tuning` 1.1.0 → 1.1.1 / versionCode 1101；
- `vm.swappiness` 100→60 并统一 apps memcg 树，减少长待机唤醒后的 zram swap-in 卡顿与压缩耗电；
- `vm.min_free_kbytes` 11.5MB→24MB，减少 direct-reclaim / allocstall 停顿，改善续航与唤醒动画流畅度。

### 1.1.0

- 版本 1.0.2 → 1.1.0 / versionCode 1100；
- 连接状态以真实蓝牙连接为准，卡片与设置页共用单一稳定连接态，不再在“电量/未连接”之间跳变；未连接时不再回退残留电量，锁屏环形电量不再误显 100%；
- 修复 LSPosed 模块加载失败：构建脚本排除 Xposed API/UEventObserver 桩类，避免编入 dex 被 LSPosed 拒绝加载；
- 吸附判定改用 pen1/pen2 霍尔对为主信号（1:1 未吸附，其余已吸附），不再用恒为 0 的 getWirelessPenPresent 误判，吸附后正确解锁 144Hz；
- 刷新率写源统一（仅 system_server 按自身霍尔写入），消除 120/144 反复切换闪屏；
- 设备卡刷新节流（1.5s），消除 notifyCardConnectionAdded 主线程刷屏，修复设备空间详情页打不开与磁吸弹窗卡顿；
- 弹窗接收端不再主线程扫蓝牙；断开/重连正确切断/恢复笔输入。

### 1.0.2

- 版本 1.0.1 → 1.0.2 / versionCode 1002；
- 刷新率：`settings_enable_oppo_pencil` 全部写入统一为“笔已连接且未磁吸”，消除 hook 与状态同步互相覆盖导致的反复 120/144 模式切换闪屏；状态变化时强制 `OplusDisplayModeService` 重算，磁吸/取下即时生效；开机前 20 秒延迟重算避免启动闪屏；
- 断开链路：移除自定义断开闩锁对原厂 `g.b` 连接的拦截，`CoreService → s0.z()/s0.x()` 全权负责连接/断开生命周期；`publishDisconnected` 不再上闩/禁用原厂笔设置，断开后重连恢复正常；
- 笔输入：断开时在 system 进程禁用 NVTCapacitivePen / Lenovo 笔输入设备，重连恢复，杜绝断开后短时仍可输入；
- 电量桥接：修复 `pencil_sp_battery_level`（原厂 PreferencesProvider 需非空 ContentValues）写入；设备卡 `x2.b.f` / `DeviceInfo.getBatteryList` 桥接 + 重建 DeviceInfo 写回 DeviceInfoManager，`mBatteryList=[100]` 已生效；
- 固件桥接：写原厂 provider 键 `pencil_sp_fw_version`（设置页固件显示真实读取源），修复固件版本 null；
- 振动：OEM s0 会话不可用时清除 `oem_control_ready` 标记并回退 PenHapticGatt 直连；
- 构建：hook APK 统一以非增量方式安装，保证 LSPosed 开机能解析模块并注入 system_server。

### 1.0.1

- 版本 1.0.0 → 1.0.1 / versionCode 1001；
- 修复 LSPosed 注入失效：hook APK 以非增量方式安装并同步模块路径，system_server 环境光桥恢复正常加载。

### 1.0.0

- 版本统一 1.0.0 / versionCode 1000；
- 清理模块 payload 中 41 个历史 dex 快照与冗余构建产物；
- 全新构建脚本（javac+d8+aapt2+baksmali+apksigner），产物集中到 `releases/`；
- 三个 APK 重新签名。

## 五、安装与作用域

1. 卸载旧包 `com.codex.*`，卸载旧模块；
2. 安装 `releases/` 下模块 zip（ksud module install）与 Hook APK；
3. 重启后在 LSPosed 管理器按各模块 `scope.list` 手动勾选作用域。

## 六、已知说明

- “无人不熄屏”为 `stay_on_while_plugged_in=15`（USB 保持唤醒）导致，非 AON 故障；关闭后正常 15s 熄屏；
- 小布 DSP（SoundTrigger/UIM）唤醒无开源替代方案，BWV CPU 路径为唯一可用实现；
- 小布说话开头偶发卡顿：暂未复现，后台日志采集已清理。

## 七、1.0.0 安装状态（2026-08-07）

- 已卸载全部 `com.codex.*` 旧包与旧模块；新包/新模块全新安装并验证：
  - `com.aclaniakea.colorosostatsguard`（1.0.0）
  - `com.aclaniakea.lenovopenbridge`（1.0.0）
  - `com.aclaniakea.penhidctl`（priv-app，1.0.0，无图标）
  - `coloros_port_base_fix` / `coloros_port_tuning` / `lenovo_pen_bridge`（1.0.0 / versionCode 1000）
- 模块服务日志正常（CPU 频点恢复、swappiness 继承、手写笔真实状态镜像）；
- LSPosed 作用域由用户按各模块 `scope.list` 手动勾选。
