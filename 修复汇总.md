# ColorOS 移植修复汇总（ACLaniakea 2.0.13）

设备：联想小新 Pro GT（TB710FU / SM8650Q / pineapple），ColorOS 16 (OPD2513) 移植，KernelSU + LSPosed。
版本：活跃合并模块 `coloros_port_fix` 为 `2.0.13`（versionCode 2013）；`base-fix/module` 与 `port-tuning` 仅保留为历史参考。作者 ACLaniakea，包名 `com.aclaniakea.*`。

协议：本项目采用 [MIT License](LICENSE)，仅供个人学习与设备移植研究使用。

## 一、基础修复（base-fix）

### KernelSU 模块 `coloros_port_base_fix`

| 修复项 | 实际作用 | 实现位置 |
| --- | --- | --- |
| AON QNN 生命周期 | 把原厂 AIBoost/QNN(HTP) 运行时 bind 进 AON 应用链接器命名空间，启动命名空间加载器，恢复真实 NPU 推理生命周期；规避移植栈 nativeDelete 崩溃 | post-fs-data.sh / aon-namespace-loader.sh / zygisk |
| AON QNN ODM 配置 | 补齐移植 ROM 缺失的 /odm/etc/camera QNN 配置 | post-fs-data.sh |
| 环境光自适应 | 恢复 oplus.product.display_features.xml，色温自适应重新生效 | post-fs-data.sh |
| 144Hz 刷新率 | 覆盖云端 60/90/120 策略，提供 TB710FU 144Hz 策略 | post-fs-data.sh |
| 显示色域映射 | 生动=原生色域(256)、自然=sRGB(0)，修正移植 ROM 色温标签偏暗 | post-fs-data.sh / system.prop |
| 录音增益 | HAL 级绑定 speaker-mic 采集增益 TX_DEC 96 / ADC 16，修复小布唤醒与录音音量偏小/破音 | post-fs-data.sh / mixer_paths |
| Tango 32 位兼容 | 停止不兼容 tango zygote，bind 翻译过的 32 位 libdl | post-fs-data.sh |
| 性能 HAL 兼容 | 使用 SoC-696 六核 targetconfig 重载并保留 perf HAL，消除缺失服务造成的 Binder 重连风暴；开机一次恢复 CPU 硬件范围，不使用轮询拉频守护 | service.sh |
| thermal 重载 | 模块挂载后重载 thermal-engine，CPU 策略生效 | service.sh |
| 小布 BWV 唤醒 | 启用/解冻 OVoice 与 SpeechAssist、写入全局唤醒开关与唤醒词、安装原厂模型、OVMS 检测窗口延长至 6000ms | service.sh |
| 电池健康入口 | 开启电池健康页开关，数据由 LSPosed 桥接真实 sysfs | service.sh |
| 序列号补齐 | ro.serialno 等空属性从真实属性继承 | common.sh |
| 应用建议修复 | 修正“应用建议”卡片协议配置 | service.sh / card-protocol-patcher.jar |

### LSPosed Hook `com.aclaniakea.colorosostatsguard`

- AonYuvLayoutBridge：归一化前置摄像头 YUV_420_888 平面布局，抑制移植 nativeDelete 崩溃，保持真实推理结果；
- AmbientColorSensorBridge：环境光色温自适应桥接（CCT 输出与 eyeprotect_display_cct 对齐）；
- ColorOSVoiceWakeupBridge：小布 BWV 唤醒链路（PCM 增益、阈值缩放、非流式完整模型、VPR 声纹 speakerId 保留、唤醒窗口与重挂时序）；
- BatteryHealthBridge / CpuHealthInfoBridge / RealtimeGpuBridge：电池健康、CPU/GPU 信息桥接；
- OStatsCpuGuard：在 `Temperature` 进入 ThermalService 缓存前，以真实 cpuss/skin 校正幽灵 `socd` 和滞留 Severe，并把禁用的 `hbm` 表面温度显示桥接到 `skin-msm-therm`；真实高温与原厂保护保持有效；
- 人脸线程优先级实验已撤销：移植平板的旧版 `libmegface` 位于 `system_server` 且 `mgu_*` 使用 nice=-10；实测限制到 nice=0 会拖慢识别，因此当前不再 Hook 人脸线程优先级，完整保留原始识别性能；
- SoundTriggerTrackerGuard / LenovoPenBridgeGuard / LenovoColorModeBridge / OcrScannerCameraBridge / CellularIdentityUiGuard / OplusSystemIdentityBridge：对应系统兼容性钩子。

## 二、调优（port-tuning）

### KernelSU 模块 `coloros_port_tuning`

- post-fs-data 阶段 bind mount 覆盖 `/my_stock/etc/extension/` 下三份 osense 配置：关闭实测会在应用切换时触发大量压缩与换入的主动 swap 策略；IO/MEM PSI 配置同时移除交互期间的高频清理规则；
- 从 post-fs-data 起，8/12GB 全局与根 memcg `swappiness=50`；普通/冷后台=50，active/systemserver/launcher/native-system=0；有界启动窗口只改变关键进程后续内存归属，始终保持 `memory.move_charge_at_immigrate=0`，不在首次解锁批量搬运已计费匿名页；
- 保留 ROM 既有 ZRAM（兼容 8/12GB RAM 机型的 0～1×RAM 配置，不创建或改容量），仅在真实内存压力下由内核按需使用；NAND swap 保持关闭，不新增常驻调优守护；
- `vm.min_free_kbytes` 保持64MB；8GB watermark=10，12GB 保留20；CMA根因修复后40/50/60交叉测试选择50，60会重新放大压缩、换入与扫描；
- 将移植手机脚本的 KGSL 单次回收从38,400页（约150MB）限制到1,024页（4MB）；保留 max_reclaim_limit 与总回收能力，仅将巨型 shrink 拆为小批次；
- 参数在开机阶段一次性写入，延迟创建的 system_server memcg 由有界等待补写后立即退出；action 按钮仅打印诊断。

## 三、手写笔桥接（pen-bridge）

### KernelSU 模块 `lenovo_pen_bridge`

- 开机按已绑定地址直接调用原厂 CoreService `CONNECT_PENCIL` BLE 连接（不以 Hall/CPS 为前提）；
- 设置页断开调用 `DISCONNECT_PENCIL`，由 Hook 执行真实 `BluetoothGatt.disconnect()`，设备/HID 真实离开连接态；
- 常驻监控真实 ACL/GATT/Hall/CPS 事件，驱动设置页卡片/磁吸胶囊/电量/存在状态（只接受真实事件）；
- CPS 开机上电：仅真实磁吸 Hall 0:1 时保持 GPIO；磁吸只负责充电与弹窗；
- PenHidCtl（priv-app，纯服务无图标）HID 连接控制，只调后台 PenHidService；
- TB710FU 刷新率策略 bind mount；
- 屏幕唤醒不再重复回放 Hall/连接状态，真实 Hall/GATT/Root 事件负责更新；`pen_wakeup_*` 节点只在开机按需写一次，避免与锁屏合成竞争。
- 相同 PEN_FRAMEWORK uevent 30 秒去重，真实状态变化仍立即分发；修复真实 Lenovo 笔事件被输入监控误过滤的问题；
- 书写触觉默认走带写回调的直连 GATT；固件以标准 2A26 为主，并跨 system_server/IPeManager 共享稳定缓存。

### priv-app `com.aclaniakea.penhidctl`（PenHidCtl）

- 纯服务应用（仅 PenHidService，无 Activity），**桌面无图标**；
- 以 priv-app 形式随模块挂载到 `/system/priv-app/aclpenhid`，持有 BLUETOOTH_PRIVILEGED / WRITE_SECURE_SETTINGS 权限；
- HID 连接/断开由 Root 服务通过 `am startservice` 调后台服务完成，无启动器入口。

### LSPosed Hook `com.aclaniakea.lenovopenbridge`

- BLE/GATT、System Server、IPeManager、Hall、设备空间与设置逻辑桥接；
- 设备空间手写笔存在键只跟随真实蓝牙连接；电量未知保留最后一次有效电量；
- 不涉及亮度/背光/屏幕电源参数。

## 四、更新日志

### 2.0.13（移植云控收口与积极 ZRAM）

- 40/50/60 共11轮交叉测试选择全局/普通后台50、动态低压档30、direct 10；50相对40中位数换入/换出下降约31%/40%、direct scan下降约45%、memory full PSI下降约23%，60则反弹。关键UI/system_server/SurfaceFlinger/Launcher继续保持0，不以强制填满ZRAM为目标。
- 关闭实际依赖缺失接口的NAND/HybridSwap、Memory Nirvana、OSense压缩和多窗口/小游戏主动清理；保留日志已确认命中的原厂预加载、标准ZRAM、LMKD、MGLRU和独立Scene调度。
- 反编译确认HMBIRD管理器即使属性为false，也会因binder stub存在而注册窗口监听。BaseFix Hook 1.1.12在SM8650Q system_server内只禁用该监听与场景开关，消除空场景空指针，不影响Scene调度或原厂perf HAL。
- FixModule更新到2.0.13；KernelSU授权allowlist在两次安装和冷启动前后哈希保持一致。

### 2.0.12（ZRAM/CMA 锁屏回收风暴）

- 轻量熄屏/亮屏复现确认约22秒内换入1.26GB、换出1.96GB、kswapd扫描7.2GB、direct reclaim扫描4.4GB；内核同时报告 zsmalloc order-0 `__GFP_CMA` 分配失败，CMA归零，锁屏关键线程阻塞在 `z_erofs_runqueue`/`folio_wait_bit_common`。
- 精确 GKI 源码确认标准 `zram.ko` 在 `zs_malloc(pool,size,gfp)` 两条路径都追加 `__GFP_CMA`。兼容层新增仅作用于 `zs_malloc` 的 kprobe，清除 zsmalloc 的 CMA 标志，不改变普通页、DMA、显示或相机分配；真机命中143,227次后 CMA 由0恢复到31MB。
- 同条件 A/B 中，换入下降约60%、换出约71%、kswapd扫描约53%、direct scan约71%，compact stall 15→9；随后11轮交叉测试确认50相对40中位数换入/换出下降约31%/40%、direct scan下降约45%、memory full PSI下降约23%，60则反弹，故最终全局值和kswapd硬上限均为50，direct保持10；不以强制填满ZRAM为目标。
- `/proc/oplus_mem/alloc_adjust_ctrl` 继续接受原厂ABI写入但永久保持0，防止OSense在不完整OPlus内存栈上重新启用高阶回收屏蔽。
- 云控审计确认 `Memory Nirvana` 在可用内存约1.55GB时触发，但其 `/dev/memcg/memory.force_shrink_file` 在联想内核缺失，实际退化为缓存进程清理；HybridSwap、zram writeback节点同样不存在，HMBIRD场景管理器还会在system_server反复空指针。模块现从特性与配置两层关闭Nirvana、NAND/HybridSwap、OSense压缩、HMBIRD高负载及多窗口/小游戏主动清理，同时保留实测成功的原厂预加载、标准ZRAM、LMKD、MGLRU和独立Scene调度。
- ZRAM不按容量百分比强制填充；普通/冷后台以50积极压缩，关键UI/system_server/SurfaceFlinger/Launcher保持0。当前约3.1GB逻辑页只占约1.2GB物理内存，后续长待机可自然增长，避免为了达到固定50%占用制造额外压缩和重大缺页。
- 移除手写笔 Root 服务与 system_server Hook 在亮屏后1.2秒对同一Hall状态的双重回放；真实Hall/GATT事件和低频对账保持不变。

### 2.0.11（标准 ZRAM 兼容与最终压力收口）

- 在精确 GKI build `13606743` / common commit `5c2cea985a84` 上构建并复验 `oplus_mm_compat.ko`；37 个外部符号 CRC 全部与官方 `Module.symvers` 一致，连续构建 SHA-256 一致，与 `oplus_shell_temp_compat.ko` 一起由 post-fs-data 一次性加载。
- 兼容层只恢复可安全外挂的 `/proc/oplus_mem` swappiness 控制面，如实声明 `backend=standard_zram`、`hybridswap=0`、`writeback=0`；不导入受限制 Android vendor hook，诊断跟踪与 allocation-adjust 默认且常态为关闭。
- 真机 11 应用压测确认当前内核标准 ZRAM 在 CMA 余量耗尽时会发生约 3.45GB 换入 / 5.1GB 换出及 zsmalloc order-0 失败；2.0.11 当时先保留 `min_free_kbytes=65536`、全局 swappiness=10 与 KGSL 单批 4MB，后续2.0.12已在修复CMA根因后重新交叉测试并改为50。
- 8GB 机型的 ActivityManager 缓存进程上限由 64 进一步收口为 48，12GB 机型保持 ROM 原值；仍由原生 ActivityManagerConstants 重算路径生效，不增加常驻守护。
- BaseFix Hook 更新到 1.1.11，FixModule 更新到 2.0.11；安装包内置同签名 Hook 和两枚已验证 KO，冷启动后可直接检查真实后端与诊断 Hook 关闭状态。

### 2.0.10（首次解锁页迁移风暴与内核兼容审计）

- 定位首次解锁 `system_server`、SurfaceFlinger、SystemUI 与 Launcher 进入 memcg 时启用 `memory.move_charge_at_immigrate=1` 会同步搬运已计费页面；实测同窗口出现约 70MiB 换入、48MiB 换出和约 360MiB kswapd 扫描。该 cgroup v1 接口已被上游标记为废弃，且本机缺少完整 OPlus HybridSwap 控制面，改为只迁移进程归属、让后续分配自然进入目标组。
- `vm.min_free_kbytes=65536` 提前到 service 起点写入，并加入现有四分钟有界启动复校；真机确认晚到的 vendor init 会在 post-fs-data 正确写入后覆盖回约11MiB，现不再让首次解锁沿用该过低水位。
- 关闭 OSense 的动态 ZRAM 与 cMem 开关；保留 Lenovo ROM 已初始化的标准 zram、MGLRU、LMKD 与内核按需回收，不伪造 `/proc/oplus_mem` 或 HybridSwap sysfs。
- 实机 CPU 容量确认是 `379 + 4×867 + 1024` 的 `1+4+1` 三簇语义，四颗同容量中核因硬件频域拆为 policy1/policy3；Scene 调度更新到 1.0.5，后台可用 CPU0-4，SurfaceFlinger 使用 CPU1-5。
- 精确反查当前 GKI 为 Google build `13606743`、common commit `5c2cea985a841939e6d074cbed2019dec0245fcd`；建立 `kernel-compat/oplus_compat`，首个 Horae `/proc/shell-temp` 兼容模块已本地编译，vermagic 与全部外部符号 CRC 匹配。真机加载后 SELinux 自动标记为 `horae_proc`，原厂 Horae 每5秒真实写入且失败重试消失，KernelSU 授权哈希不变；现由 FixModule 在 post-fs-data 一次性加载。
- 源系统 OPD2513 对应一加平板 3 Pro / SM8850，后续接口基准改用一加官方 `sm8850_b_16.0_pad_3_pro` 分支，不再把 SM8650 一加平板 2 源码误当作原系统同版本。
- 完整风险分层、源码基线和未来内核合并路线见 [内核兼容性与后续移植说明.md](内核兼容性与后续移植说明.md)。

### 2.0.8（手写笔状态/触觉与 Launcher 长待机）

- 修正手写笔广播把普通事件无条件提升为“已连接”、仅配对即视为在线的问题；断开时向原厂 DeviceInfo/BATTERY_NOTIFY 明确发布 `connected=0`、`battery=-1`、`charging=0`，不再让锁屏/控制中心卡片保留上一会话的 100%。
- 触觉命令只交给原厂 IPeManager `s0` GATT 会话；会话尚未 ready 时有界合并首笔命令，禁止 `system_server` 建立第二 GATT，避免随机失效及核心进程风险。原厂服务发现后补发 switch/request 初始化并回放仍有效命令。
- 设备卡详情页保留可见期 400ms 刷新，后台取消永久 5 秒主线程轮询，状态更新依赖原厂事件回调。
- 实机长待机现场确认 Launcher 独自留在 root memcg 且 `VmSwap≈296MB`，而 system_server/SystemUI/SurfaceFlinger 已为 0；开机有界窗口新增一次性 launcher memcg 与匿名页迁移，稳定态 `swappiness=0`，不新增常驻守护。
- FixModule 更新到 2.0.8，PenBridge Hook/Module 更新到 1.1.15。

### 2.0.7（长待机唤醒换入风暴）

- 6小时40分长待机现场确认：ZRAM 使用约3.8GB，累计换入约40GB、重大缺页约1090万次；system_server/SystemUI/SurfaceFlinger 已分别换出约243/154/43MB。
- 在 post-fs-data 提前创建原厂命名的 active/systemserver memcg，并在 system_server、SystemUI、SurfaceFlinger 出现时启用一次 `move_charge_at_immigrate` 完成进程与匿名页计费迁移，随即关闭该开关；native system、active、systemserver 使用0，普通与冷后台统一10。
- 8GB机型当时将源手机96个缓存进程折中限制为64，后续 2.0.11 压测收口到48；12GB机型保持原值。Hook ActivityManagerConstants 的原生重算路径，不增加常驻守护。
- 手写笔 Hook/Root 的 Hall、屏幕与蓝牙全量查询改为事件优先、低频校验；Root 模块内置同签名 Hook 副本并以事务固定 LSPosed 路径，保留振动、电量、充电与首次吸附逻辑。
- BaseFix Hook 更新到1.1.9，FixModule更新到2.0.7，PenBridge Hook/Module更新到1.1.10；Provider 隔离 system_server 后复用原厂 IPeManager GATT 会话，消除第二客户端抢占并避免首笔事件导致核心用户空间重启。

### 2.0.6（恢复原厂音效互斥）

- 恢复“声场扩展”和“空间音频”的原厂双向互斥：尊重 Settings 的拒绝结果，不再由 Hook 强制覆盖为模式 3。
- 区分 MSS 借用的系统 Spatializer 与真正的外层“空间音频”模式，打开页面时自动修复旧版本遗留的双开状态，并保留此前的杜比/原始模式。
- BaseFix Hook 更新到 1.1.8，FixModule 更新到 2.0.6。

### 2.0.5（GPU 回收风暴与首次解锁调优）

- 定位 `system_server` / `SurfaceFlinger` / `kswapd` 同时抬升的共同触发器：移植手机脚本把 KGSL `page_reclaim_per_call` 设为 38,400 页（约 150MB），一次 GPU shrink 会触发大批量页回收、ZRAM 换出与图形资源重建；
- 首轮将 KGSL 从 38,400 页降到4,096页已使三次设置页/桌面切换的 `kswapd` 扫描约2.19GB→94MB、换出约581MB→11MB；进一步冷启动压测发现4,096页在低余量时仍可复发多GB回收；
- 最终将 KGSL 单次回收设为1,024页（4MB）。3次切换约67MB换出、855MB kswapd扫描、183MB direct reclaim；A2恢复4,096页后重新升到约742MB、3.16GB、1.05GB，`system_server` CPU 用时约8.16→14.57秒，排除热缓存偏差；
- 全局 swappiness=5 在热缓存短窗口内表现较好，但冷启动后5次切换出现约618MB换出、3.8GB扫描和231MB direct reclaim；全局20进一步放大换出，故最终全局/根 memcg取10、8GB watermark=10，12GB保留watermark=20；
- 修正 cgroup v1 根 memcg 别名陷阱：该内核写 `/dev/memcg/memory.swappiness` 会同步改写全局 `vm.swappiness`，根组现与全局同为10，apps子树仍按10/20分层；
- 不新增常驻调度守护，不改 cpuset/uclamp/WALT，保留原厂动态 boost、真实温控和人脸识别 nice=-10；撤销会拖慢人脸识别的 FaceThreadPriorityGuard 实验。
- FixModule 安装、post-fs-data 与稳定态均自动补齐 BaseFix 的 LSPosed 必需作用域；同步仅增补而不删除用户自定义作用域。
- BaseFix Hook 更新到 1.1.7，FixModule 更新到 2.0.5。

### SM8650Q 专用 Scene 调度模块 1.0.5

- 新增完全独立的 KernelSU 模块 `sm8650q_scene_scheduler`，通过 UPerf/Scene 标准 `/data/powercfg.sh`、`powercfg-base.sh`、`powercfg.json` 接口工作，Scene 直接显示为“SM8650Q 专用调度”；
- 仅接受 `SM8650Q/pineapple`、CPU `0-5`、cpufreq `policy0/1/3/5` 的真实六核拓扑，拒绝 8 核或其它同平台设备误用；
- 修正 Scene 熄屏 `standby` 被映射为省电模式的问题：待机保持均衡基线，由内核 suspend 与原厂 Power HAL 负责实际省电，避免亮屏解锁动画在前台模式回调前继承低频上限和关闭输入 boost；
- 按实机 `cpu_capacity=379/867/867/867/867/1024` 采用 `1+4+1` 三簇调度语义；policy1/3 只是四颗同容量中核的两个频域，后台使用 CPU0-4，144Hz SurfaceFlinger 使用 CPU1-5，避免闲置两颗中核后在解锁时集中清积压；
- 提供 powersave/balance/performance/fast 四档 WALT 上限、升降频响应、短时输入 boost 与迁移阈值；性能/极速实载验证 prime 核可从 480MHz 升至真实 boost 最高频 3302.4MHz；
- Scene 分应用实测：绑定 fast 的应用进入时自动调用 `fast` 并开放 3302.4MHz，退出回桌面自动调用 `balance` 恢复 2995.2MHz；UI 来源设为 `SOURCE_OUTSIDE` 后不再与在线 HP 的 8 核配置互相覆盖；
- 不运行额外常驻进程，不改 ZRAM/内存策略，不固定 CPU 最低频率，不停止原厂 Power/Perf HAL，不关闭或绕过任何温控。
- service 仅在 `boot_completed` 后等待 15 秒复用 Scene 当前模式并退出，避开修复模块重载 perf HAL/thermal-engine 的约 9 秒启动窗口；不延后 system_server，也不形成常驻复校或模块间硬依赖。

### 2.0.4（长待机/冷启动 system_server 定向修复）

- 实测首次解锁时 CE 应用集中恢复，system_server 瞬时约 140%、kswapd 最高约 75%，ZRAM 在约 38 秒由 0 增至约 1GB；全 0 A/B 虽阻止换出，却令 kswapd 升至约 90% 且持续更久，故最终取消 `boot_completed` 后额外 8 秒 service 延后，并用最长四分钟的有界窗口把新建 app memcg 直接分层为 active/system_server=0、普通=10、冷后台=20，不增加常驻守护；
- 对照 12GB 原厂 PKX110：原厂全局/所有 memcg 均为 100、12GB ZRAM 已用约 6.9GB 仍保持低 PSI，证明目标应是避免回收风暴而不是禁止 ZRAM；两机均有“首次解锁后 600000ms 不主动裁剪超额缓存进程”，模块仅对 ≤9GB 机型改用新版 AOSP 默认 0ms，12GB 继续保留原厂策略；

- 线程级实测定位亮屏尖峰：`system_server` 的 `mgu_prepare` 连续占用约 34%～60% 单核，`mgu_compare/mgu_detect` 同时运行；平板加载 `/system/lib64/libmegface.so` 和 `libFaceDetectCA.so`，却不存在原厂设备的独立 AIDL `face_hal`；
- 原厂对照机的人脸推理由独立 `face_hal` 承担，库不映射进 `system_server`。移植平板的 `mgu_*` 工作线程还被设置为 nice=-10，直接与 PowerManager、Display、Launcher/SF 动画抢占；
- 曾测试 FaceThreadPriorityGuard 限制 `mgu_*` 优先级；因 nice=0 会让人脸识别明显变慢，最终版本已完整撤销该 Hook，恢复原始 nice=-10；
- service 在 `boot_completed` 后立即进入，不再额外等待 8 秒；已运行的 OVoice 不再强停重启，Hook dexopt 只在安装时执行，减少无功能收益的重复 PackageManager 工作；
- 应用建议协议从 WAL 变化触发的每分钟修补改为开机修补 + 30 分钟低频兜底；OVoice 保留“熄屏省电核、亮屏全核”行为，但改为阻塞订阅 `screen_toggled/am_proc_start`，仅在真实边沿读取一次状态，不再每 10 秒 `dumpsys power`；
- 移除 OSense 30 分钟熄屏强制压缩、空闲压缩和主动 compact；启动前 90 秒保持关键 memcg 防换出，稳定态普通/冷后台 10/20、active/system_server=0，兼容 8GB/12GB 与 ROM 自带任意 0～1×RAM ZRAM；
- BaseFix Hook 更新到 1.1.6，FixModule 更新到 2.0.4。

### 2.0.2（温感映射与交互负载修正）

- 保留 `vendor.perfservice` / `perf2-hal` 和修正后的 SoC-696 六核 topology；开机仅一次恢复 governor 与硬件 min/max。实机停止额外守护后四簇 `scaling_max_freq` 仍保持 2265600/2956800/2956800/3302400，因此不再用常驻脚本和原厂 Thermal/Power HAL 争抢上限；
- 删除实验性的 2 秒 `cpu-maxfreq-keeper`。它会在 HAL 报告 Severe 时仍反复写满频，可能覆盖真实温控，而且额外制造常驻 shell 唤醒；动态 boost、低电量限制和真实过热降频继续由原厂 WALT/perf/thermal 链处理；
- 手写笔监控降载：screen fallback 从 250ms `dumpsys power` 改为 2s、Bluetooth 全量 dumpsys 从 1s 改为 5s、Hall 去抖从 4Hz/3 样本改为 2Hz/2 样本、CPS GPIO fallback 从 1s 改为 5s；真实 Hook/GATT/Hall 边沿仍即时处理，低频轮询只负责状态兜底；
- 手写笔 OEM GATT 写队列加入 1.8s 回调 watchdog。若进程仍在但 BluetoothGatt 回调链已失活，自动清除假 ready 并让下一次命令转入已验证的 Lenovo 直连 GATT，避免队列永久 busy 导致振动时好时坏。

#### 六核频点表（SoC-696）实测

- 6 核版与 8 核版的差异是**核心数（6 vs 8），不是频点表**；prime 核频点表一致：
  - nominal（`scaling_available_frequencies` 上限）：little 2265600 / big 2956800 / titanium 2956800 / **prime 3052800**；
  - boost（`scaling_boost_frequencies`）：prime = **3110400 3187200 3244800 3302400**；
  - `cpuinfo_max_freq` = 3302400 真实存在，检测软件读到 3302 是对的；满载+boost 才能上到 3.3GHz，普通满载约 3.24GHz；
- 所以"上不去最高频"的根因是云控/均衡锁频（2.44GHz），不是 6 核频点表缺失。

#### 温度传感器映射问题（已修）

- 根因：源手机 thermal 配置里的 `sensor rear-tof-therm` 是源机的**后置 ToF 摄像头**温度传感器，平板根本没有这个硬件，读到的 29.5°C/80°C 是噪声；thermal-engine 却拿它做 CPU 四簇和电池温控的触发源，导致误触发限频；
- 修复：`thermal-engine_cpu_0.conf` 的四个 CPU 簇传感器改为真实存在的 **`cpuss-0/1/2/3`**（CPU 簇温）；`thermal-engine_battery_*.conf` 的 `[MON-BATTERY-USER]` 改为 **`skin-msm-therm`**（真实皮温）。模块内已无任何 `rear-tof-therm` 引用；
- 真正的框架幽灵项是 **`socd`**：对应 thermal zone 在真实 skin 约 38°C、`cpuss-0..3` 约 50°C 时仍返回整数 92~94，移植 HAL 因源机阈值 90 把它标成 Severe(3)。BaseFix 仅对精确名称 `socd` 向 Android 客户端桥接四个真实 cpuss 温度的最大值，并只在最大簇温 ≤80°C 时清除幽灵 Severe；物理值读不到时完全保留原厂结果，不伪造固定温度；
- `hbm_temp/user_temp/user_back_temp` 当前同源且 thermal zone 为 disabled、HAL 状态为 0，不参与 CPU 限频。保留原厂 HBM feature，不再把 hbm 伪装成 skin，也不使用固定 40°C fallback；
- `skin-msm-therm` 是可用真实皮温，内核 trip 实测为动态 48/95/60°C 结构，并非固定 46.5/48°C 两级。删除按索引写 55/57°C 的脚本，保留内核、thermal-engine 和电池全部安全保护；只在物理 skin 已降到 43°C 以下时清除移植 HAL 无 cold threshold 导致的陈旧状态。

#### 全部温控传感器分类（SoC-696 六核）

| 类别 | 传感器 | 实读值 | 是否真实 | 处理 |
| --- | --- | --- | --- | --- |
| CPU 簇温 | cpuss-0/1/2/3 | 43~47°C | ✅ 真实 | 用作 CPU 温控源（已换入） |
| CPU 核温 | cpu-0-0-0、cpu-1-0-0/1、cpu-1-1-0/1、cpu-2-0-0/1、cpu-2-1-0/1 | 42~48°C | ✅ 真实 | 保留 |
| CPU 拓扑冗余热区 | cpu-0-1-0、cpu-1-2-0/1/2、cpu-2-2-0/1 | 43~47°C | ⚠️ 名称继承 8 核 DT，仍可能映射物理 TSENS | 不作为本模块策略源，保留 95°C+ 原厂保护 |
| NPU/DSP（nsp） | nsphvx-0/1、nsphmx-0~3 | 42~44°C | ✅ 真实（Hexagon） | 保留（trip 100°C+） |
| GPU | gpuss-0~7 | 41~44°C | ✅ 真实 | 保留 |
| Modem | mdmss-0~3 | 40~42°C | ✅ 真实 | 保留 |
| PMIC | pm8550_tz、pm8550b_lite_tz、pm8550ve_tz、vs_d/e | 39~46°C | ✅ 真实 | 保留（trip 95°C+） |
| 充电/电池 | pm8550b-chg-therm、ichg、ufs、battery | 36~39°C | ✅ 真实 | 保留 |
| 皮温 | skin-msm-therm | 38~45°C | ✅ 真实 | 保留动态内核 trip 与原厂保护；仅清理冷却后的 HAL 陈旧状态 |
| 后置 ToF 摄像头 | rear-tof-therm | 29.5°C | ❌ 无此硬件 | **已从 thermal 配置移除** |
| 虚拟 SoC 温度 | socd | 92~94（整数） | ❌ 与真实 cpuss/skin 脱钩 | 框架层桥接真实 cpuss 最大值；失败时不改原值 |
| HBM/用户表面虚拟温 | hbm_temp、user_temp、user_back_temp | 同值 22~26°C | ⚠️ 虚拟热区、disabled | 不参与 CPU 策略，不改值、不禁用显示功能 |
| 其它源机件 | xo-therm/cam-flash/wlan/usb/wls/wireless/pa-therm | 29.5°C/-40°C | ❌ 无此硬件 | 不进 thermal 配置，仅显示 |
| 虚拟表面温 | user_temp/user_front_temp/user_back_temp | 16~25°C | ⚠️ OPlus 温控 HAL 计算值，映射错（前表面 16°C 明显偏低） | 仅显示，待 HAL 侧校正 |

### 2.0.1（性能 HAL 修复 + CPU 调速器恢复）

- 长待机唤醒卡顿的实测根因不是内存，而是第三方调频器 Uperf Game Turbo（由 Scene/`com.omarea.vtools` 控制）在熄屏时把四个频点簇的 governor 全部切到 `powersave` 并把 min=max 钉在最低频（prime 核 787MHz、大核 1.19GHz）；
- 更严重的实测根因：模块此前「停用 perf2-hal / vendor.perfservice」是错的。显示 composer/框架每次大合成都会通过 perfservice 发 CPU 提示，服务被停用后变成「perf aidl service doesn't exist」的失败 AIDL 重连风暴（5 秒 600+ 次），把 system_server 的 binder 线程打满，CPU PSI some 冲到 88%、load 30+。修复：改用修正后的 pineapple SoC-696 六核 targetconfig 重载这两个 HAL 并保持运行，风暴归零、PSI 回落到 20 左右，频点仍保持硬件上限；
- 服务阶段在原有 max_freq 恢复基础上，追加 min_freq 恢复与 governor 归一（仅纠正 powersave→schedutil/walt），min/max 频率节点 0444 需先 chmod 再写；不新增常驻守卫/轮询；
- 根因在 Uperf 的熄屏切 powersave，建议停用 Uperf（或关闭 Scene 熄屏自动切 powersave），本模块只做开机一次性恢复。

### 2.0.0（合并修复模块）

- 修复 1.2.x 合并回归：此前 apps 父分组及应用子 memcg 实际仍为 `swappiness=100`；实机 A/B 压测后采用开机根/apps=5，稳定后普通=10、冷后台=20、活跃与 system_server=5，并关闭会放大交互换页的 OSense 主动换出；
- 修复 systemserver cgroup 延迟创建、错过 service 一次性写入的问题：加入 1 秒低频、命中即退出、最长 120 秒的有界修正，不增加常驻调优守护；
- `vm.min_free_kbytes` 32MB→64MB，在 8GB 设备上提前唤醒 kswapd，减少 UI 分配进入 direct reclaim/allocstall；
- AON namespace loader 从每 50ms `pidof`（20Hz）改为阻塞监听 ActivityManager `am_proc_start` 事件；实测旧实现 4 小时消耗约 7.5 分钟 CPU/52 万次调度，新实现空闲 20 秒 CPU 与调度计数均零，同时可在 AON 新 PID 上完成真实 QNN runtime bind；
- 把 apps 父 memcg 与 64MB `min_free_kbytes` 前移到 post-fs-data 首段，并同步修正高通开机脚本的全部水位写入，避免开机前一分钟仍以 ROM 的 5–11MB 水位发生 direct reclaim；
- 自动恢复被精简策略禁用的原厂 `com.oplus.romupdate`，修复 Scene“关闭无障碍服务”开关的 `Could not find provider`，不覆盖用户自身开关值；
- 冷启动显式启用并 unstop 外部 Dolby Bridge 包/服务组件，再配置 idle 与 appops 白名单；修复重装或重启后包处于 stopped 状态、原厂设置页能显示但隐式 Binder 服务无法启动的问题；
- 杜比 Bridge 同步 `system_dolby` 与外层 `system_effect_profile`，避免 DAX 已启用而音效页仍显示“原始”；场景、GEQ 与 10 段自定义值均由 Settings 持久化并在服务重建时重新写入 DAX；
- 杜比模式 UI 进一步同步原厂观察器使用的 `Settings.Secure.dolby_switch`：选择“原始”后强制收起并隐藏杜比场景/EQ 分类，选择杜比时恢复显示；拦截模型回调和 Secure 状态回放，避免旧值把子菜单重新拉起，并对重复写入做防递归处理；
- 手写笔模块普通广播进程不再硬依赖 LSPosed 日志类，修复 Hall/电池广播偶发进程崩溃导致控制中心设备卡片刷新不稳定；
- 手写笔振动的 OEM GATT 就绪状态绑定当前 IPeManager PID，开机清理上次会话标记；原厂进程/会话不存在时自动回退直连 GATT，并对特征值/描述符写入失败进行有界重试，避免重启后命令落入失效广播而无振动；
- 手写笔满电时将内核 `CHARGING_STATE=Full` 稳定映射为未充电；充电监控同时比对硬件真值和 UI 镜像，即使硬件值没有边沿变化，也会纠正被原厂状态回放改回“充电中”的控制中心缓存；持久化及卡片读取统一通过 Hall/OEM 硬件状态归一化；
- 修复 QTI Thermal HAL 虚拟 skin 与设备物理皮温偏差过大、降温后仍长期保持 Severe(3) 的移植问题：system_server 仅在真实 `skin-msm-therm` ≤43°C 时清除陈旧状态，保留 46.5°C Severe 触发线及全部底层 thermal-engine/内核保护，不增加常驻拉频服务；
- action 诊断补充 apps 根 memcg 与 CPU/内存/I/O PSI，卸载时恢复 ROM memcg=100 和默认 11584KB 空闲水位；
- 现场 4 应用驻留后 3 轮切换对比主动换出方案：swap-in 约减少 75%、swap-out 约减少 64%、direct scan 约减少 95%，第 3 轮启动耗时为 244/254/161/170ms；首次同时装入大型应用仍会发生真实内存压力，未通过危险温控放宽掩盖。

### 1.1.5（调优模块）

- `coloros_port_tuning` 1.1.4 → 1.1.5 / versionCode 1105；
- 定位关键机制：本内核全局回收不遵守 per-memcg swappiness（cgroup v1 无内存限制时按全局值从全局 LRU 换出），per-memcg 10 无法阻止关键进程被换出；
- 全局 `vm.swappiness` 40→20（实测关键进程换出立即停止增长并回落），post-fs-data 阶段即生效。

### 1.1.4（调优模块）

- `coloros_port_tuning` 1.1.3 → 1.1.4 / versionCode 1104；
- 当时定位的剩余卡顿：保护只在 service 阶段（boot_completed+8s）生效，开机早期以 ROM 默认 swappiness=100 已把关键进程换出约 570MB；
- post-fs-data 阶段尽早降全局 swappiness 并写关键 memcg，杜绝开机早期换出窗口；
- 追加禁用 `sys_osense_memory_decisionmaker_config.xml` 的 `reentrant/memstop`、`zram`、`mem_leak` 清理规则（此前 MEM PSI 仍触发 reentrant）；
- 移除模块内从未生效的 `my_stock/` 死配置。

### 1.1.3（调优模块）

- `coloros_port_tuning` 1.1.2 → 1.1.3 / versionCode 1103；
- 实测定位剩余卡顿来源：即使关键进程防换出生效，系统仍持续约 120MB/s 换出、76MB/s 换入（10s 内 3.1 万页换出），zram 抖动是动画拖慢/掉帧主因；
- post-fs-data bind mount 禁用 osense 后台应用主动 zram 换出（swap `switch=false`）并移除 IO PSI `reentrant/iostop` 高频清理；
- 关键 memcg swappiness 20→10、后台 60→40、`min_free_kbytes` 32MB→64MB，进一步压缩换页抖动。

### 1.1.2（调优模块）

- `coloros_port_tuning` 1.1.1 → 1.1.2 / versionCode 1102；
- 实测定位长待机唤醒卡顿根因：zram 中 system_server/launcher/systemui 被换出约 800MB，点亮时全部 swap-in 造成动画拖慢掉帧；
- `active`/`systemserver` memcg `swappiness` 20（防换出），后台 apps 保持 60；`min_free_kbytes` 提升到 32MB 防 direct-reclaim 停顿；
- 新增常驻守护每 30s 复校关键参数，防 osense 回改。

### 1.1.1（调优模块）

- `coloros_port_tuning` 1.1.0 → 1.1.1 / versionCode 1101；
- `vm.swappiness` 100→60 并统一 apps memcg 树，减少长待机唤醒后的 zram swap-in 卡顿与压缩耗电；
- `vm.min_free_kbytes` 11.5MB→24MB，减少 direct-reclaim / allocstall 停顿，改善续航与唤醒动画流畅度。

### 1.1.0

- 版本 1.0.2 → 1.1.0 / versionCode 1100；
- 连接状态以真实蓝牙连接为准，卡片与设置页共用单一稳定连接态，不再在“电量/未连接”之间跳变；未连接时不再回退残留电量，锁屏环形电量不再误显 100%；
- 修复 LSPosed 模块加载失败：构建脚本排除 Xposed API/UEventObserver 桩类，避免编入 dex 被 LSPosed 拒绝加载；
- 吸附判定改用 pen1/pen2 霍尔对为主信号（1:1 未吸附，其余已吸附），不再用恒为 0 的 getWirelessPenPresent 误判，吸附后正确解锁 144Hz；
- 刷新率写源统一（仅 system_server 按自身霍尔写入），消除 120/144 反复切换闪屏；
- 设备卡刷新节流（1.5s），消除 notifyCardConnectionAdded 主线程刷屏，修复设备空间详情页打不开与磁吸弹窗卡顿；
- 弹窗接收端不再主线程扫蓝牙；断开/重连正确切断/恢复笔输入。

### 1.0.2

- 版本 1.0.1 → 1.0.2 / versionCode 1002；
- 刷新率：`settings_enable_oppo_pencil` 全部写入统一为“笔已连接且未磁吸”，消除 hook 与状态同步互相覆盖导致的反复 120/144 模式切换闪屏；状态变化时强制 `OplusDisplayModeService` 重算，磁吸/取下即时生效；开机前 20 秒延迟重算避免启动闪屏；
- 断开链路：移除自定义断开闩锁对原厂 `g.b` 连接的拦截，`CoreService → s0.z()/s0.x()` 全权负责连接/断开生命周期；`publishDisconnected` 不再上闩/禁用原厂笔设置，断开后重连恢复正常；
- 笔输入：断开时在 system 进程禁用 NVTCapacitivePen / Lenovo 笔输入设备，重连恢复，杜绝断开后短时仍可输入；
- 电量桥接：修复 `pencil_sp_battery_level`（原厂 PreferencesProvider 需非空 ContentValues）写入；设备卡 `x2.b.f` / `DeviceInfo.getBatteryList` 桥接 + 重建 DeviceInfo 写回 DeviceInfoManager，`mBatteryList=[100]` 已生效；
- 固件桥接：写原厂 provider 键 `pencil_sp_fw_version`（设置页固件显示真实读取源），修复固件版本 null；
- 振动：OEM s0 会话不可用时清除 `oem_control_ready` 标记并回退 PenHapticGatt 直连；
- 构建：hook APK 统一以非增量方式安装，保证 LSPosed 开机能解析模块并注入 system_server。

### 1.0.1

- 版本 1.0.0 → 1.0.1 / versionCode 1001；
- 修复 LSPosed 注入失效：hook APK 以非增量方式安装并同步模块路径，system_server 环境光桥恢复正常加载。

### 1.0.0

- 版本统一 1.0.0 / versionCode 1000；
- 清理模块 payload 中 41 个历史 dex 快照与冗余构建产物；
- 全新构建脚本（javac+d8+aapt2+baksmali+apksigner），产物集中到 `releases/`；
- 三个 APK 重新签名。

## 五、安装与作用域

1. 卸载旧包 `com.codex.*`，卸载旧模块；
2. 安装 `releases/` 下模块 zip（ksud module install）与 Hook APK；
3. 重启后在 LSPosed 管理器按各模块 `scope.list` 手动勾选作用域。

## 六、已知说明

- “无人不熄屏”为 `stay_on_while_plugged_in=15`（USB 保持唤醒）导致，非 AON 故障；关闭后正常 15s 熄屏；
- 小布 DSP（SoundTrigger/UIM）唤醒无开源替代方案，BWV CPU 路径为唯一可用实现；
- 小布说话开头偶发卡顿：暂未复现，后台日志采集已清理。

## 七、1.0.0 安装状态（2026-08-07）

- 已卸载全部 `com.codex.*` 旧包与旧模块；新包/新模块全新安装并验证：
  - `com.aclaniakea.colorosostatsguard`（1.0.0）
  - `com.aclaniakea.lenovopenbridge`（1.0.0）
  - `com.aclaniakea.penhidctl`（priv-app，1.0.0，无图标）
  - `coloros_port_base_fix` / `coloros_port_tuning` / `lenovo_pen_bridge`（1.0.0 / versionCode 1000）
- 模块服务日志正常（CPU 频点恢复、swappiness 继承、手写笔真实状态镜像）；
- LSPosed 作用域由用户按各模块 `scope.list` 手动勾选。

## 2.0.14 / 调度 1.0.8 — 网络软中断与特性表

### WLAN 收包软中断从弱核 CPU0 分流（调度模块 1.0.8）

实机 `/proc/interrupts` 显示 WLAN 的 14 条中断把 `smp_affinity_list` 显式钉在 `0`，
合计约 43.6 万次全部落在唯一弱核 CPU0（容量 379），`msm-vidc` 与 6 条 `i2c_geni`
同样有效落在 CPU0；同期 CPU3/CPU4 各只有约 10 万次。投屏这类场景要同时吃 Wi-Fi
数据面与硬件视频编码，两者却挤在同一颗核上。

WLAN 的硬中断无法迁移：QCA 驱动为其置了 `IRQ_NO_BALANCING`，实测对 irq 308/319/326
写 `smp_affinity` 与 `smp_affinity_list` 一律返回 rc=1/EIO。因此改走 RPS，只搬随后的
softirq 协议栈处理，不触碰硬中断。

原厂 `init.qcom.post_boot.sh` 只为 `rmnet0` / `rmnet_ipa0` 配置了 RPS，`wlan*` 从未
配置——在源机八核上驱动自身的 NAPI 亲和会把中断铺到大核簇，用不着 RPS；搬到本机
`1+4+1` 六核后既被钉死在 CPU0，又没有 RPS 兜底。这是移植后内核与用户空间的对齐缺口。

模块现在把 `wlan0` / `p2p0` 各 5 个 rx 队列的 `rps_cpus` 设为 `1e`（CPU1-4，Prime CPU5
不参与），`rps_flow_cnt=4096`，全局 `rps_sock_flow_entries=32768`。实测 A/B（相同
ping 负载各 20 秒，`/proc/net/softnet_stat` processed 增量）：

| | CPU0 | CPU1 | CPU2 | CPU3 | CPU4 | 合计 |
| --- | --- | --- | --- | --- | --- | --- |
| RPS 关闭 | 1875 (63.5%) | 46 | 47 | 418 | 556 | 2944 |
| RPS = CPU1-4 | **101 (3.4%)** | 1489 | 37 | 55 | 1270 | 2952 |

CPU0 下降约 94.6%，总量基本不变，属纯粹重分布。`wlan0`/`p2p0` 被重建时 `rps_cpus`
会清零，Scene 每次切换模式都会调用同一入口顺手补写；已是目标值时只读不写，不新增
常驻监听。

同时把 `msm-vidc` 移到 CPU2 与其固件接口 `hfi` 同核（避免每帧跨核），6 条 `i2c_geni`
统一移到 CPU1。

### OPlus 特性表改为增量派生（修复模块 2.0.14）

旧实现把仓库内一份手工维护的 48 条快照 bind 到 `/my_stock/etc/extension/
com.oplus.oplus-feature.xml`。这是整表替换：原厂表 52 条，凡未被收录的条目一并消失，
且 OTA 更换原厂表后会静默回退到旧快照、丢掉新增特性。

现在改为运行时读原厂表 + 声明式增删：补 `oplus.software.audio.dolby_support`，删掉
缺内核控制面的 `memory_nirvana.enable` / `highload_pause_hyb_swapd` /
`osense.compress.enable` 与源手机的 `psi_multi_window_clean` / `psi_miniprogram_clean`，
其余原样保留。派生后校验条目数方向与幅度，任一不满足就完全不 bind、保持原厂行为。

派生结果与原快照的特性集完全一致（实机 `52 -> 48 entries`），属零行为变化的重构。

### 设备空间手写笔菜单连接状态反复横跳（Hook + 笔桥接模块）

见 commit `f88986c`：连接镜像键有三个写者且无所有者、无去抖；`DISCONNECT_PENCIL`
不写镜像导致 UI 最长滞后 30 秒，而对账器会把用户半秒前刚设的断开闩锁当成陈旧闩锁
清掉，自动重连随即把 UI 翻回已连接。现由 Hook 立即写镜像并打用户意图时间戳，
对账器在 8 秒有界窗口内不发布、不清闩锁，收敛后回到 30 秒低频对账。

## 调度 1.0.9 — 弱核上迁门槛按 1+4+1 拓扑重定

### 现象

系统动画掉帧、应用启动时六核全满载且很卡。实机冷启动 `com.android.settings`
`TotalTime` 中位约 1136 ms，`dumpsys gfxinfo` 50% janky、50 分位 109 ms、
90 分位 300 ms，而 GPU 各分位只有 2 ms —— **瓶颈完全在 CPU，不在 GPU、不在合成**。

### 排除过的方向

| 方向 | 结论 |
| --- | --- |
| ART boot image 失效、framework 退化解释执行 | **不成立**。zygote64 的 maps 里 `boot-*.art/oat` 齐全，framework 是 AOT 的 |
| dexopt 不足（245 个包停在 `verify`） | **不成立**。把 Settings 强制编译成 `speed` 后，冷启动 1397 ms vs 1302 ms，无改善 |
| cpuset 掩码错配 | **不成立**。`top-app=0-5`、`sf=1-5`（已排除弱核）、`foreground=0-5`，均合理 |
| 停用 OPlus performance HAL | **反而更慢**。中位 999 ms → 1152 ms（+15%），停用期间日志 37 条崩溃/服务不可用。它虽然一直在刷 `sched_assist` 缺失错误，但通过其他存在的路径确实在工作，与 HybridSwap 那类"控制面完全不存在"不同，不可照搬处理 |

### 根因

WALT `sched_upmigrate` 向量三位分别对应 CPU0→中核、中核→中核、中核→X4。
源系统均衡档第一位是 90：SM8850 有四颗小核可以铺开，等任务吃满一颗小核的
90% 再上迁是合理的。本机只有 CPU0 一颗弱核（容量 379，中核 867、X4 1024），
90% 意味着它必然先饱和才开始搬运。

实机证据：空闲采样 `cpu0 busy=62.2%` 而 `cpu5 busy=7.0%`，`policy5` 在 60 个
采样里 42 次停在最低频 480000；冷启动压力下六核全满载但 `cpu0=95.6%`。

### 验证与改动

交叉采样（6 轮轮换臂顺序、取中位，消除"越跑越热"的缓存效应；早期顺序测试
得出的 -44% 大部分是该效应造成的假象）：

| 设置 | 中位 |
| --- | --- |
| 基线 `[90 95 82]` | 1136 ms |
| 只降第一位 `[60 95 82]` | 939 ms |
| 三位全降 `[60 60 60]` | 942 ms |

只降第一位与全降在统计上不可区分，**收益完全来自弱核这一个边界**。因此保留
后两位：中核之间同容量，压低只增抖动；通往 X4 的门槛另有功耗取舍。

四个档位的第一位相应下调（均衡档 60 为实测值，其余按同方向外推、未单独实测）：

| 档位 | 改动前 up | 改动后 up |
| --- | --- | --- |
| 省电 | `95 95 95` | `85 95 95` |
| 均衡 | `90 95 82` | `60 95 82` |
| 性能 | `85 90 72` | `55 90 72` |
| 极速 | `80 85 65` | `50 85 65` |

装机重启后实测（6 次冷启动期间）：

| | 改动前 | 改动后 |
| --- | --- | --- |
| cpu0 | 95.6% | **46.2%** |
| cpu5 | 7.0% | 24.9% |
| cpu1-4 | 18~28% | 21~24% |
| 冷启动中位 | 1136 ms | **875 ms** |

