# 4.1.1 卡顿与内存回收排查（2026-09-15，进行中）

起点：怀疑卡顿来自 HybridSwap 与对照机 PKX110 的逻辑不一致。本文记实测数据，
**包含被证伪和尚未证实的部分**。

## 一、已证实：`swapd_pause` 的移植语义与原厂相反

### 对照机实测（三轮，100 ms 采样 `bl_power` 与 `hybridswap_swapd_pause`）

| 轮次 | 亮屏时刻 | `pause=1` | 延迟 | `pause=0` | 持续 |
|---|---|---|---|---|---|
| 1 | t=3.5 s | t=4.8 s | +1.3 s | t=10.7 s | 5.9 s |
| 2 | t=47.5 s | t=48.6 s | +1.1 s | t=54.7 s | 6.1 s |
| 3 | t=30.4 s | t=31.9 s | +1.5 s | t=37.8 s | 5.9 s |

**原厂行为是：屏幕亮起约 1.2 秒后才把 swapd 暂停，压住约 6 秒再放开。**
不是熄屏暂停，而是保护亮屏/解锁后的第一个交互窗口。
并非每次亮屏都触发，观察到的间隔下限约 84 秒内不会二次触发（疑似冷却）。

写这个节点的是 `/odm/bin/hw/vendor-oplus-hardware-performance-V1-service`。
已核实**两台跑的是同一个二进制**（对照机上该文件同样存在，`strings` 里有
`hybridswap_swapd_pause`）。ROM 里除 `Athena.apk` 外没有第二个写入者。

### 与本机的差异

4.0.4 把"屏已亮而 `pause` 仍为 1、约 5 秒后才释放"判定为移植造成的时序错位，
并在 4.1.0 中让面板桥接管后**永久把该节点钉死为 0**：

```c
if (hybridswapd_panel_bridge_active()) {
        atomic_set(hybridswapd_ops->swapd_pause, 0);
        return len;   /* 丢弃 HAL 的写入 */
}
```

对照上表，那个"延迟"就是原厂设计本身。后果是对照机 `swapd_manual_pause`
累计 2.55 亿次（约占 wakeup 调用的 58%），平板恒为 **0**。

**处置**：已恢复原厂 setter（删除该覆盖、`hybridswapd_panel_bridge_active()`
与 `panel_event_seen`），面板 kprobe 维持 `display_off` 不变。重编产物通过
`kernel-compat/tools/verify_hybridswap_task_abi.py`；与出货件相比指令流仅
`hybridswap_swapd_pause_store` 内少 3 条（`bl`/`tst`/`csel`），其余全是地址位移。
**尚未上机验证。**

## 二、已证伪：swapd 不是本机回收的主力

压力基准（清缓存 + 杀后台 → 依次拉起 10 个应用 → 桌面翻页 30 次）下，
HybridSwap 自己几乎不干活，回收全由 kswapd 与**直接回收**承担：

| | A1 | B1 | A2 | B2 |
|---|---:|---:|---:|---:|
| `pgsteal_direct` | 1,685,224 | 1,334,373 | 2,666,189 | 3,421,479 |
| `pgsteal_kswapd` | 1,979,749 | 1,884,125 | 2,253,199 | 2,316,958 |
| `allocstall` | 27,593 | 21,990 | 44,014 | 56,323 |
| `workingset_refault_anon` | 695,548 | 634,759 | 933,796 | 1,096,480 |
| `swapd_swapout`（HybridSwap） | 21 | 32 | 28 | 32 |

**直接回收占总回收的 40~60%**，每轮数万次 `allocstall`；而 HybridSwap 的
`swapd_swapout` 只有 20~30 次。

> **更正（同日晚些时候）**：本节初稿据 `workingset_refault_anon / pswpin`
> 在平板恒为 100%、对照机为 72%，断言平板在严重抖动。**该结论作废。**
> 6.1 的 `workingset_refault()` 对每个带 shadow 的换入都计一次
> `WORKINGSET_REFAULT_ANON`，与"是否仍在工作集内"无关；真正的判据是
> `workingset_activate_anon`。两机实测：
>
> | | `activate_anon` | `refault_anon` | 占比 |
> |---|---:|---:|---:|
> | 对照机 PKX110 | 25,322,373 | 162,551,092 | **15.6%** |
> | 平板 | 2,104,714 | 13,437,323 | **15.7%** |
>
> 两机的真实重入率**完全一致**。100% 那个数字是 6.1 与 6.6 的计数口径差异
> （对照机跑 6.6.89，28% 的换入没有 shadow），不是抖动差异。

因此 `swapd_pause` 这条虽然确实与原厂不一致，**不足以解释卡顿**；它改变的是
一个本来就几乎不工作的旁路。

## 三、发现但尚未证实：平板加载了对照机没有的四个内存策略模块

| 模块 | 对照机是否出货 | 对照机是否加载 |
|---|---|---|
| `oplus_mm_gloom` | 否 | 否 |
| `oplus_mm_levelprotect` | 否 | 否 |
| `oplus_mm_memload_opt_mapped_protect` | 否 | 否 |
| `oplus_mm_process_reclaim` | 否 | 否 |

对照机 `/vendor_dlkm/lib/modules/` 共 161 个 oplus 模块，**不含**上述四个
（`sigkill_diagnosis`、`abort_mm_opt`、`memleak_detect` 出货但也各有取舍）。
它们来自我们自己的源码普查，不是对照机的配置。

其中 `mapped_protect` 在本项目的编译配置下（未定义 `CONFIG_MAPPED_PROTECT_ALL`）
只注册一个钩子 `android_vh_page_referenced_check_bypass`，语义是
**mapcount ≥ 20 的页直接跳过 referenced 检查**——即被多进程共享的页免于回收，
压力因此转向匿名页。`gloom` 则需要一个 203 KB 的内核补丁往 mm 热路径插钩子。

### A/B 结果：本轮无效，不能下结论

在线 `rmmod` 四个模块后重跑同一基准，四轮数据（上表 A1/B1/A2/B2）呈**单调恶化**，
与是否加载模块无关——说明 `am kill-all` + `drop_caches` 并不能把 swap 占用与
内存碎片恢复到同一起点，历史效应完全淹没了组间差异。

**有效的 A/B 必须每轮重启**。为此给 `oplus-bsp-module/post-fs-data.sh` 加了
`$MODDIR/skip-modules`（每行一个模块名，本次开机跳过，删文件即恢复），
不必改清单本身。

## 四、顺带确认与排除

* 桌面翻页在四轮里都不卡：p95 帧时 10.3~12.5 ms，只有 A1 出现过一帧 78.6 ms。
  **当前基准没有复现用户所说的卡顿**，需要用户指认真实场景。
* hybridswap 参数两机一致：`avail_buffers` 2500/2200/2500、`free_swap_threshold` 1536。
* 仍不一致的 vm 参数：`min_free_kbytes` 11584 / 23168，`watermark_scale_factor` 80 / 50。
  算下来两机的 low watermark 接近（72.6 MB vs 79 MB），暂不认为是主因。
* 平板 `MemTotal` 7.76 GB，对照机 11.38 GB，ROM 同为 ColorOS 16。

## 六、新发现：zram_opt 的 swappiness 比对照机高得多（尚未 A/B）

`/proc/sys/vm/swappiness` 是摆设（4.0.4 已查明，zram_opt 的
`android_vh_tune_swappiness` 在回收路径上无条件覆盖）。真正生效的值在
`/sys/module/oplus_bsp_zram_opt/parameters/`，两机对比：

| 参数 | 作用路径 | 对照机 PKX110 | 平板 |
|---|---|---:|---:|
| `vm_swappiness` | kswapd / 通用回收 | 125 | **160** |
| `hybridswapd_swappiness` | HybridSwap swapd | 125 | **200** |
| `vm_swappiness_threshold1/2` | 分档 | 125 / 125 | **0 / 0** |
| `direct_vm_swappiness` | 直接回收 | 60 | 60 |
| `oplus_extra_free_kbytes` | 额外空闲水位 | 0 | **无此参数** |
| `dynamic_direct_swappiness` | 动态直接回收档 | 0 | **无此参数** |

两边都没有用户态写入者（ROM 里引用 `/proc/oplus_mem` 的三个组件
——performance HAL、Athena、`autochmod.sh`——的字符串里都没有 `swappiness_para`）。
所以两个值都是**各自模块的编译期默认值**：我们这份源码快照写死
`g_swappiness = 160`、`g_hybridswapd_swappiness = 200`（`zram_opt.c:26,44`），
而对照机那份是 125。

也就是说，平板的 kswapd 比对照机**多 28% 倾向于换出匿名页**，HybridSwap 的
swapd 多 **60%**。这与实测症状直接对应：每轮基准换出 110~180 万页，
`workingset_refault_anon` 与 `pswpin` **逐次完全相等**（100% 换入都是重入），
对照机同一比值为 72%。

参数是 `S_IRUGO|S_IWUSR`，可直接写 `/sys/module/.../parameters/` 在线验证。

另外对照机的 zram_opt 明显是更新的版本：多出 `mask_kswapd_reclaim`、
`costly_alloc_mask_reclaim`、`dynamic_direct_swappiness` 三个 `/proc/oplus_mem`
节点，`swappiness_para` 的权限也不同（0664 vs 我们的 0666）。

### 与之相关：两份 hybridswap 模块的编译配置不同

把对照机的 `/vendor_dlkm/lib/modules/oplus_bsp_hybridswap_zram.ko` 取下来对比：

* 对照机编进了 `hybridswapd_chp.c`（有 `zram_used_limit_mb_read/write`，
  运行态也确有 `/dev/memcg/memory.zram_used_limit_mb` 节点），我们的 Makefile 不含该文件；
* 反过来我们编进了 `kcompressd.c`（71 处字符串），对照机**一处都没有**；
  两机运行态都没有 kcompressd 线程，所以这条目前只是配置差异，未见行为差异；
* 除此之外函数符号只差 7 个，其中 `bright_fb_notifier_callback`
  是面板通知器分支（我们走 kprobe），其余是内联差异。

即：**两份模块的主体逻辑已经一致**，剩下的差异集中在编译开关和 zram_opt 的调参上。

## 五、待办

1. **swappiness 对齐对照机**（160/200 -> 125/125），在线可改，优先级最高。
2. 重启隔离的 A/B，确认第三节四个模块的真实影响。
3. 上机验证第一节的 r1 模块。
4. 让用户指认卡顿的具体场景，据此重做基准。


## 七、swappiness A/B：无可靠效果（2026-09-15）

用同一段连续压力内交替切换的办法（每相 60 秒，交替六相，抗历史漂移）实测
`vm_swappiness` 160→125 与 `hybridswapd_swappiness` 200→125：

| 相 | swappiness | 总回收(页) | 直接回收占比 | allocstall | 换出/总回收 |
|---|---|---:|---:|---:|---:|
| 1 | 160 | 5,421,026 | 47.9% | 42,594 | 29.3% |
| 2 | 125 | 5,678,511 | 49.3% | 46,034 | 28.5% |
| 3 | 160 | 4,843,258 | 53.5% | 42,602 | 32.0% |
| 4 | 125 | 5,201,966 | 40.4% | 34,774 | 28.1% |
| 5 | 160 | 4,291,963 | 43.4% | 30,530 | 31.5% |
| 6 | 125 | 5,129,229 | 49.5% | 41,977 | 31.0% |

均值 160 组换出占比 30.9%、125 组 29.2%，但第 6 相（125）的 31.0% 高于第 1 相
（160）的 29.3%，**分布重叠，不构成可靠差异**。直接回收占比与 allocstall 同样重叠。

**结论：swappiness 这条只有"对齐对照机"的保真理由，没有实测收益。**
不要把它写成性能修复。

## 八、THP：唯一有硬数据的策略分歧

平板 `/sys/kernel/mm/transparent_hugepage/enabled = [always]`，对照机 `= never`。
两边的 `defrag`、`khugepaged/*`、`compaction_proactiveness` 全部一致。

来源查清了，是两家 vendor 分区的策略相反：

| | 脚本 | 函数 | 写入 |
|---|---|---|---|
| 平板（联想 vendor） | `/vendor/bin/init.kernel.post_boot-memory.sh` | `enable_thp()` | `echo always` |
| 对照机（OPlus vendor） | 同名脚本 | `configure_thp()`，注释 "disable THP by default" | `echo never` |

两边都由 init 服务 `memory-post-boot` 执行
（`/vendor/etc/init/hw/init.qti.kernel.rc`）。我们的 `fix-module` payload 里那份
`init.kernel.post_boot.sh` 写的是 `never`，但它管不到这个独立服务，所以被覆盖。

实测代价（平板开机 1 小时）：

| | 平板 | 对照机 |
|---|---:|---:|
| `thp_fault_alloc` | 8,334 | 0 |
| `thp_fault_fallback` | **406,637**（约 112 次/秒） | 0 |
| `AnonHugePages` | 2,048 kB | 0 kB |

8,334 个成功分配的 2MB 大页，最后只剩 1 个还在——其余全被拆掉了。
也就是说这套机制在本机**只产生开销、不留下收益**。

> 这里要更正 4.0.4 的一条方法论：当时对 `watermark_scale_factor` 与
> `min_free_kbytes` 的处置理由是"这是这台硬件的 vendor 原厂值，不动"。
> 但本机的 vendor 分区是**联想**的、按 ZUI 调的，而跑在上面的 system 是
> ColorOS。"原厂"在这里有两个，且策略相反。判断某项该不该动，要看它属于
> 硬件约束（时钟、拓扑、功耗墙）还是内存策略——后者应当跟 ColorOS 走。

顺带查明：OPlus 的 `configure_min_free_kbytes()` 对 **RAM ≥ 8GB 一律给
`min_free_kbytes = 23168`**（仅 25600/24600 系列产品降到 11584）；联想那份写死
11584 并配 `watermark_scale_factor=80`。本机 7.76 GB 按 OPlus 口径算作 8GB 档。
**但换算下来两机的 low/high 水位相差不大（平板 73/134 MB，对照机 60/100 MB），
方向也与"减少直接回收"相反，因此本轮不动，只记录。**

### 处置

`fix-module/module/post-fs-data.sh` 新增 `align_thp_to_coloros()`：运行时从原厂
脚本派生一份、只把那一条 `always` 改成 `never`，校验确实改到且行数不变后 bind。
派生失败、没改到、或原厂脚本本来就不写 `always`，则完全不动。

## 九、决定性对比：同一套压力下两机的回收行为（2026-09-15）

前面几节的 A/B 全部落在噪声里，原因是**没有对照基线**——不知道平板的数字到底
算不算异常。补上了：同一个压力脚本（同样 9 个应用循环拉起）、同样 60 秒窗口，
分别在两台上跑。

| 60 秒窗口 | 对照机 PKX110（11.4 GB） | 平板 TB710FU（7.8 GB） |
|---|---|---|
| 总回收页数 | 1,526,180 / 621,640 | 4,949,637 ~ 5,874,195 |
| **直接回收占比** | **27.3% / 11.8%** | **50.4% ~ 62.8%** |
| **allocstall** | **6,859 / 1,202** | **41,078 ~ 60,945** |
| 换出页数 | 353,256 / 107,319 | 1,476,752 ~ 1,662,632 |
| 真重入率（activate/refault） | 19.2% / 23.9% | 12.9% ~ 14.8% |

* 平板回收量是对照机的 **3.5~9 倍**，直接回收占比 **2~5 倍**，
  `allocstall` **6~50 倍**。
* `allocstall` 就是"某个线程不得不自己下去回收内存"的次数：平板约
  **800~1000 次/秒**，对照机 20~114 次/秒。**这就是卡顿的直接机制。**
* 对照机第二个窗口已经收敛（62 万页、11.8% 直接），平板**六个窗口全部维持在
  500 万页、55% 直接**——**平板从头到尾没有进入稳态。**
* 平板的真重入率反而**更低**。它不是"换出去的页马上要回来"，而是单纯在做
  数倍的回收工作。

## 十、根因指向：不可回收内存占比

| | 对照机 | 平板 |
|---|---:|---:|
| MemTotal | 11.38 GB | 7.76 GB |
| **Unevictable** | **71 MB** | **740 MB** |
| **Shmem** | **118 MB** | **766 MB** |
| KGSL `page_alloc` | 619 MB | 760 MB |
| 锁定合计 / 物理内存 | 189 MB / **1.7%** | 1.51 GB / **19%** |
| **应用可用内存（估）** | **≈ 11.19 GB** | **≈ 6.26 GB** |

两台申请的 GPU 显存量级相当（619 MB vs 760 MB），但**对照机只有 71 MB 落在
Unevictable，平板是 740 MB**——平板的 KGSL 内存全部被钉住。

去掉锁定部分后，两机的应用可用内存相差 **1.8 倍**。物理内存只差 1.46 倍，
`allocstall` 却差 6~50 倍——正是越过拐点之后的非线性表现。

### 这推翻了 4.1.0 对 KGSL 回收链的处置理由

4.1.0 做过一个内核桥补上 `state` 写入者（熄屏实测释放 450~510 MB），最后以两条
理由移除：**(1) 原厂不用这套；(2) 待机腾出的内存没有消费者，收益要在下一次解锁
时偿还（桌面 95 分位帧时 24ms -> 101~121ms）**。

理由 (2) 现在站不住了：**消费者就是前台负载本身**。当时只测了"熄屏全量回收 ->
解锁"这一种最坏用法，没有测"亮屏、有压力时只回收后台进程的显存"——而后者正是
联想 `msm_kgsl` 那套 process reclaim 的设计用途（`kgsl_reclaim_shrink_scan_objects`
由 `do_shrink_slab` 在真实压力下调用）。

理由 (1) 仍然成立但需要重新权衡：对照机不需要这套，是因为它的 KGSL 内存本来
就没被钉住（71 MB）。平板被钉住 740 MB 是移植造成的状态，不是"原厂就这样"。

**下一步应当是：只在亮屏且有内存压力时，把 `oom_score_adj` 高的后台进程标记为
可回收，交给内核 shrinker 自行决定回收多少；前台与常驻系统进程始终保持 PINNED。**
这与 4.1.0 测过的"熄屏全量回收"是两种不同的策略，不能用当时的数据否定它。

## 十一、本轮所有 A/B 的诚实结论

| 改动 | 与对照机的差异 | 实测效果 |
|---|---|---|
| `swapd_pause` 恢复原厂语义 | 确凿（对照机 2.55 亿次 vs 平板 0） | swapd 每轮只换出 20~30 次，**无可测效果** |
| `swappiness` 160/200 -> 125/125 | 确凿（编译期默认值之差） | 六相交替，分布重叠，**无可靠效果** |
| THP `always` -> `never` | 确凿（两家 vendor 策略相反） | 六相交替去趋势后残差 ±8000，**无可测效果**；唯一确定收益是每分钟少 1.87 万次徒劳的 2MB 分配尝试 |
| 四个多余 mm 模块 | 确凿（对照机既不出货也不加载） | **尚未做重启隔离的 A/B** |

**三项已完成的对齐都是保真修复，不是性能修复。真正的量级差异在第十节。**

## 十二、三条对齐已固化并实机验证（2026-09-15）

| 项 | 验证结果 |
|---|---|
| `swapd_pause` 交还原厂 setter | `swapd_manual_pause` 恒 0 -> 70 秒内 **+189,331**；采样可见 HAL 在亮/熄屏前后各置 1 一次再放开 |
| `swappiness` 125/125 | `/sys/module/oplus_bsp_zram_opt/parameters/` 读到 125/125/60；`.data` 段核对 `0x7d/0x7d/0x3c` |
| THP `never` | `thp_fault_alloc` 8,334 -> **0**，`thp_fault_fallback` 406,637 -> **0** |

两个 `.ko` 用新建的 `out-r2` 编译，vermagic 与运行内核逐字节一致；hybridswap 过 ABI 门禁。
开机后 490 模块、0 失败、`dmesg` 无 panic、governor=`walt`、`vendor.post_boot.parsed=1`。

> **验证陷阱（我先踩了一次）**：不要在 `sys.boot_completed=1` 的瞬间验证。
> vendor post-boot 链正是由该属性触发，那一刻还没跑完，会读到
> `governor=performance`、`min_free_kbytes=73728`、`vendor.post_boot.parsed` 为空的
> 过渡态。我据此误判为"bind 打断了 vendor 启动链"并做了一次无谓回滚；1 分钟后
> 复查各项全部正常。

## 十三、根因定位：桌面进程的 Skia 模糊渲染目标（进行中）

按进程列出 KGSL 显存（`/sys/class/kgsl/kgsl/proc/<pid>/gpumem_mapped`）：

| 显存 | state | adj | 进程 |
|---:|---|---:|---|
| **911 MB** | foreground | 0 | **com.android.launcher** |
| 188 MB | foreground | -1000 | surfaceflinger |
| 89 MB | foreground | -900 | system_server |
| 58 MB | foreground | 950 | com.perol.play.pixez |
| 54 MB | foreground | 100 | com.heytap.colorfulengine（壁纸） |
| 27 MB | foreground | 475 | com.example.piliplus |
| 25 MB | foreground | -800 | com.android.systemui |
| …… | | | |
| **合计 1.38 GB** | | | 同期 `Unevictable` 1.42 GB、`Shmem` 1.50 GB |

**全部进程都是 `state=foreground`、`gpumem_reclaimed=0`** —— 再次确认回收链第一环是断的。
按 `oom_score_adj >= 700` 能拿的只有 pixez 58 MB + devcheck 15 MB + chrome 15 MB ≈ 88 MB，
**真正的大头在桌面自己身上**。

### 桌面那一 GB 是什么

`dumpsys gfxinfo com.android.launcher` 峰值报 **1,025.23 MB（仅 117.44 MB 可回收）**，
按标签聚合 3,304 个 GPU 资源、770 MB：

| 条数 | 合计 | 标签 |
|---:|---:|---|
| 773 | 324.7 MB | `MakeSurfaceDrawContextWithFallback` |
| 821 | 155.0 MB | `SurfaceDrawContext_ConvolveGaussian/ResourceAllocatorRegister` |
| 994 | 148.2 MB | `SurfaceDrawContext_ConvolveGaussian2d` |
| 326 | 38.9 MB | `SurfaceDrawContext_ConvolveGaussian` |
| 175 | 31.4 MB | `RescaledSurfaceDrawContext/ResourceAllocatorRegister` |

分辨率分布：512×512 占 315 MB、1024×1024 占 260 MB。
**2,141 个是高斯模糊的中间渲染目标**（`ConvolveGaussian*` 是 Skia 的模糊实现），
而且其中绝大部分**不可回收**（in-use），Skia 的缓存上限对它们不起作用。

按 HWUI 的 `SURFACE_SIZE_MULTIPLIER`（48 B/px）算，本机 2000×3200 的 Skia 资源缓存
上限应为约 **307 MB**，实测峰值是它的 3.3 倍。

### 波动而非单调泄漏

同一进程多次采样：911 MB -> 1.19 GB -> 563 MB -> 514 MB -> 298 MB；
`Unevictable` 同步 1.49 GB -> 1.0 GB -> 834 MB。熄屏/`onTrimMemory` 会大幅回落。
所以不是单调泄漏，而是**交互期堆积到 1 GB 量级、且全程钉在 Unevictable**。

### 下一步需要的对照

对照机 PKX110 的桌面进程持有多少 GPU 显存、其中多少是 `ConvolveGaussian`。
注意两机屏幕像素数相差 2 倍（2000×3200 = 6.4 Mpx vs 1216×2640 = 3.2 Mpx），
模糊中间目标本就该按面积缩放，**2 倍以内属于正常，需要看实际倍数**。
该对比未做——取数时对照机已拔线。

## 十四、两机同负载对照：根因是「钉住」，不是「用得多」（2026-09-15）

同一个脚本（依次 monkey 拉起 10 个共有应用、回桌面，重复三轮），两台同时跑。
测的是 `com.android.launcher` 进程的 KGSL 显存、Skia 资源构成，以及全局 `Unevictable`。

| | 对照机 PKX110 | 平板 TB710FU |
|---|---|---|
| launcher KGSL | 203 → 210 → 206 → 206 MB | 632 → **924** → 358 MB |
| Skia 资源条数 | 523 → 392 → 391 → 391 | 1637 → 1192 → 372 |
| 高斯模糊中间面 | 13 → 13 → 8 → 7 | 426 → **646** → 3 |
| `MakeSurfaceDrawContextWithFallback` | 1 → 1 → 4 → 4 | 86 → 89 → 27 |
| **`Unevictable`** | **69 → 69 → 69 → 69 MB** | **1231 → 1567 → 1031 MB** |

**对照机在 30 次应用启动里 `Unevictable` 一动不动，恒为 69 MB**，而它同期的 KGSL
分配总量约 500 MB —— 也就是说**对照机的 GPU 显存根本不钉在 Unevictable 上**。
平板则是 1:1 全钉，于是桌面那阵发性的 ~1 GB 直接变成 1 GB 不可回收内存。

平板第 2 轮从 924 MB 崩到 358 MB，`launcher` pid 未变（6338，没被击杀），
是 HWUI 在内存压力下整体 trim —— 也就是说这块内存**只能整体丢、不能按需回收**。

### 根因拆成两层，可分别处理

**(a) 钉住（放大器，主因）**：联想这版 `msm_kgsl` 把显存分配钉成 unevictable，
只能靠 `state=background` 的 process reclaim 解钉；移植包缺了写这个节点的组件，
于是永远解不开。对照机走的是另一代驱动，压根不钉（`proc/<pid>/` 下连 `state`
与 `gpumem_reclaimed` 节点都没有）。

**(b) 分配量（次因）**：平板桌面确实分配得多——模糊中间面 646 vs 13、
`WithFallback` 89 vs 1。其中一部分是正当的（屏幕像素 2 倍、平板桌面有常驻
任务栏与模糊背景），但 `MakeSurfaceDrawContextWithFallback` 两机相差 89 倍
不能用像素数解释，值得单独查。

**只修 (a) 就能让 (b) 的阵发变得无害**：内核可以在压力下回收它们，而不是像现在
这样只能等 HWUI 整体 trim。而且判据从此很明确——**对照机的 `Unevictable` 上限
是 69 MB，平板应当向这个数靠**，而不是 4.1.0 当时用的"待机能腾出多少 MB"。

### 这条推翻了 4.1.0 移除 KGSL 桥的两条理由

* 理由 (1)「原厂不用这套」：对照机不需要，是因为它**根本不钉**；平板被钉住
  1.0~1.6 GB 是移植造成的状态，不是"原厂就这样"。两者不是同一件事。
* 理由 (2)「待机腾出的内存没有消费者」：消费者就是前台负载本身。第九节实测
  平板 `allocstall` 约 800~1000 次/秒、对照机 20~114 次/秒。

### 顺带排除：预编译产物没有损坏

`com.android.launcher` / `com.android.systemui` / `com.heytap.colorfulengine` 三者
dexopt 状态与对照机逐项相同（`speed-profile` / `speed`），进程内 odex/vdex/art
映射段数也相同（12/32/149 等），没有解释执行。全局分布：

| status | 平板 | 对照机 |
|---|---:|---:|
| verify | 178 | 438 |
| speed-profile | 99 | 148 |
| speed | 43 | 58 |
| run-from-apk | **2** | 33 |
| error | **2** | 25 |

平板的 `error` 只有 2 条且都是 `com.heytap.reader`（非 UI 关键路径）。
4.0.4 修的那批 SYSTEMSERVERCLASSPATH odex 问题一直保持着。

### 一条需要更正的我自己的推断

本文第十三节据一次快照写下"桌面持有 1 GB Skia 模糊面"，并暗示是常态。
实测更正：**刚重启后桌面稳定在 176~203 MB，翻页、开抽屉、最近任务往返全程
模糊面 0~4 个，完全不涨**；只有反复启动应用才会堆起来（一轮 10 次 +292 MB、
+220 个模糊面）。所以那 1 GB 的快照是我自己那轮一小时压力测试逼出来的峰值，
不是静置常态——但它**确实由正常使用（频繁开应用）触发**，不是纯人造场景。


## 十五、撤回：`debug.hwui.app_memory_policy=lowram` 无效（2026-09-15）

上一节曾据一次读数认为该属性把 HWUI 的 Skia 资源缓存预算从 921.60 MB 降到
368.64 MB，并据此给出一组"收益"数据。**该结论作废。**

决定性验证（两种取值各测「刚重启」与「画过全屏后」两点）：

```
[lowram]  刚重启:     Max surface area: 6400000 | Max resource usage: 921.60MB (x144)
[lowram]  画过全屏后: Max surface area: 6400000 | Max resource usage: 921.60MB (x144)
[default] 刚重启:     Max surface area: 6400000 | Max resource usage: 921.60MB (x144)
[default] 画过全屏后: Max surface area: 6400000 | Max resource usage: 921.60MB (x144)
```

两种取值、两个时点，四次读数完全一致，**乘数始终是 x144**。该属性在本 build
上对预算没有任何影响（字符串 `lowram` 确实存在于 libhwui，但要么这版不解析它，
要么它只影响别的量）。

### 两处取数错误，记下来免得重犯

1. **`Max surface area` 是"该进程见过的最大 surface"，不是屏幕尺寸。**
   刚重启的 launcher 还没画过全屏窗口时它是 2,560,000，预算随之算成
   368.64 MB。我把这个瞬时值当成了 lowram 的效果。
   **判据应当看乘数（x144）有没有变，而不是看结果 MB 数。**

2. **那组"收益"数据是冷热状态差，不是策略差。** lowram 那轮起点是刚
   force-stop 重启的 launcher（15 MB / 模糊 0 个），default 那轮起点是已经
   跑过一小时压力测试的热状态（632 MB / 模糊 426 个）。两轮起点不同，
   后续差异无法归因于策略。

   **凡是要对比 HWUI 缓存行为，两臂都必须从 force-stop 后的冷进程开始。**

### 仍然成立的部分

预算公式本身与两机的数值对比不受影响，那是直接读出来的：

| | 对照机 PKX110 | 平板 TB710FU |
|---|---|---|
| Max surface area | 3,210,240 px | 6,400,000 px |
| Max resource usage | 462.27 MB (x144) | 921.60 MB (x144) |
| 占物理内存 | 4.1% | **11.9%** |

"公式只按屏幕面积缩放、不看物理内存，本机落在大屏+小内存的角上"这个判断成立；
**只是目前还没有找到能调它的旋钮。** 后续可考虑的方向：
* 查这版 libhwui 里 `MemoryPolicy` 的解析入口，确认它到底认哪些字符串；
* 或者绕开预算，直接解决"可回收却被钉住"——即 KGSL process reclaim。

## 16. v4.1.1 实施：shrinker 驱动的 KGSL 回收（2026-09-15）

### 16.1 方案改版

v4.1.0 的熄屏全量回收副作用明确：解锁时桌面 95 分位帧时从 24ms 飙到 101~121ms（4~5 倍）。改为 v4.1.1：

**核心改动：**
- 删除熄屏回收逻辑（panel kprobe、screen_off_sweep、延迟参数）
- 注册 shrinker，内核内存压力时触发全量标记（adj>=100 → background）
- 添加节流机制（默认 30 秒），避免 shrinker 连续打进来时重复全量扫
- adj_threshold 默认值从 800 改为 100（VISIBLE_APP）
- 仍保留 oom_score_adj tracepoint，能抓到新启动应用的 adj 变化

**理念：**
零主动轮询，不依赖屏幕状态，只在内核真有压力时动作，更接近对照机手机的行为（那边 GPU 内存本就不钉，压力来时内核自己回收）。

### 16.2 实施记录

**编译环境：**
- 内核源码：r3 out-r3（从 `kernel-build-r3-20260914.tar.zst` 归档恢复到 `/home/ACLaniakea/`）
- 修复：`register_shrinker()` 在 6.1 内核需要第二个参数（shrinker 名字）

```c
ret = register_shrinker(&kgsl_state_shrinker, "oplus_kgsl_state_bridge");
```

**刷入：**
1. r3 内核通过 EDL 9008 刷入 boot_a（`boot-droidspaces-r3-20260914.img`）
2. fix-module 更新推送（含 `oplus_kgsl_state_bridge.ko` + SELinux 规则）
3. 重启验证

**内核版本：**
```
Linux version 6.1.128-android14-11-sm8650q-droidspaces-r3
#1 SMP PREEMPT Mon Sep 14 15:00:00 CST 2026
```

### 16.3 验证结果

**模块加载：**
```
$ adb shell lsmod | grep kgsl
oplus_kgsl_state_bridge    20480  0 
msm_kgsl             1912832  25
```

**统计信息（启动后 5 分钟）：**
```
events:          3831    # oom_score_adj tracepoint 触发次数
writes:           426    # 成功写入节点次数
missing:         3993    # 节点不存在（进程已退出，正常）
failed:             0    # 写入失败（SELinux 规则生效！）
slots_full:         0    # 槽位从未满
shrinker_calls:  2505    # shrinker 被内核调用次数
shrinker_sweeps:   15    # 实际触发全量扫描次数（节流生效）
```

**进程标记验证：**
```
# 桌面在前台时
$ adb shell cat /proc/6632/oom_score_adj; cat /sys/class/kgsl/kgsl/proc/6632/state
0
foreground

# 打开设置应用后（桌面被遮挡）
$ adb shell cat /proc/6632/oom_score_adj; cat /sys/class/kgsl/kgsl/proc/6632/state
100
background
```

launcher 被遮挡后 adj 从 0 变成 100，KGSL state 从 foreground 变成 background。**桥的 adj→state 映射工作正确。**

**Unevictable 内存（启动后 5 分钟）：**
```
MemTotal:        7763404 kB
Unevictable:      544024 kB  (531 MB)
```

对比：
- v4.1.1 当前：531 MB
- 对照机基线：69 MB
- **仍有 462 MB 差距**

### 16.4 分析

**桥机制已工作：**
- SELinux 规则生效（failed=0）
- oom_score_adj tracepoint 正常触发
- shrinker 持续被调用（2505 次），全量扫描 15 次（节流正常）
- 进程标记逻辑正确（adj 100 → background）

**Unevictable 仍高的可能原因：**
1. **观察时间太短**：KGSL 的 background_work 在标记后约 12 秒才真正回收，且分批进行。5 分钟可能不足以看到稳态。
2. **回收触发条件**：shrinker 只在内核**真有压力**时才调用 scan_objects 回收。轻度使用时可能一直在 count_objects（报告可回收量）但不执行实际回收。
3. **其他 Unevictable 来源**：不全是 KGSL。可能有 mlock、ramfs、shmem 等其他来源。
4. **对照机的差异**：对照机可能**根本不钉** GPU 内存，而不是"钉了再回收"——那是架构级的差异，桥无法完全抹平。

**下一步方向：**
- 长时间观察（过夜待机、重度使用一天）
- 压力测试验证回收效果（启动多个 GPU 密集应用）
- 分析 Unevictable 来源（不全是 KGSL 的话，可能需要其他优化）
- 考虑更激进参数（adj_threshold 降到 0？节流缩短？）但要权衡功耗与响应

**关键提交：**
- 2f0b1ba1: feat(kgsl): v4.1.1 改用 shrinker 驱动，去除熄屏回收机制
- 2f4be2b7: build(kgsl): 编译 v4.1.1 模块并集成到 fix-module


## 17. v4.1.2：前摄灯失效与待机后动画卡顿（2026-09-15 晚）

### 17.1 前摄指示灯：system_server 跑的是 09-02 的旧 Hook

现象：CameraService 钩子装上了，`updateActivityCount` 调用栈里也有 LSPosed，
ZUI 相机打开 cameraId=1，却一条 `front indicator` 日志都没有。

根因不是代码：

* 机器上 FixModule 有两份（`coloros_port_fix` + 开发推送的 `acl-coloros-pad-fixes`），
  BSP 也有两份（`aclaniakea_oplus_bsp` + `oplus-bsp-module`），id 相同、脚本都在跑，
  常驻进程成对出现。
* 两份 FixModule 都在 zygote 前把 LSPosed 模块路径钉到自己目录下的 APK，谁晚谁赢。
  开发推送那份带的是仓库里 `fix-module/module/hook/BaseFix-Hook.apk`——被 gitignore、
  停在 2026-09-02，`hook/versionCode` 也停在 301115（实际 APK 是 410000）。
* 证据：运行中的 system_server 日志 `AonSmartFaceGazeCompat: start mapped to 0x60001`、
  `installed timeout-aware…` 只存在于旧 APK 的 dex；4.1.0 APK 打的是 `restored native 0x60007`。
  前摄灯的事件驱动代码 09-04 才加入，旧 APK 没有；09-13 撤回的"缓存进程上限压到 48"
  也被旧 APK 带回来了（`cached-process ceiling -1 -> 48`）。

处置：
* 设备上两份重复目录移到 `/data/adb/modules-dup-20260915/`，正式目录同步仓库脚本。
* `build_fix.py`：`hook/versionCode` 改从 Hook manifest 生成；打包时同步开发树里的
  APK 与 versionCode，开发推送不会再带旧 Hook。
* `LsposedPathSync`：当前钉住的是另一份仍启用的模块副本、且其 versionCode >= 自己时，
  拒绝改写并在日志里提示删除重复目录；PackageManager 路径（/data/app）照旧改写。

验证：重启后 system_server 不再出现旧 APK 字符串；打开前摄日志
`front indicator=on client=com.zui.camera` / `off`，实物灯亮灭正常。

### 17.2 待机后动画卡顿：v4.1.1 的 tracepoint 在熄屏后十几秒回收整个桌面

4.1.1 自称"只在压力下回收"，但 `oom_score_adj_update` 探针在每次 adj 变到 >=100
时立即写 background。熄屏后桌面被锁屏遮住、adj=100：

| 时刻 | 桌面 `gpumem_reclaimed` |
|---|---|
| 亮屏前 | 0 |
| 亮屏 +0.4s | 30 MB |
| 亮屏 +12s | 0（重新钉回） |
| **熄屏 +20s** | **115 MB（114 MB 全部）** |

解锁时这 115MB 从 zram 同步读回，与 4.1.0 熄屏回收（p95 24ms -> 101~121ms）同一机制，
只是从 30 分钟提前到十几秒。常驻 adj 100~200 的 `com.oplus.blur`（6/6MB）、壁纸
（8MB）、侧边栏（2/2MB）、输入法（6/6MB）也一直处于被回收状态。另外 shrinker 的
`count_objects` 里同步做全量扫描（每次 332 个进程的 `filp_open`），落在触发直接回收的线程上。

### 17.3 v4.1.2 策略（对照机经验）

对照机 PKX110：GPU 显存不钉、压力下按页回收；前台 UI 栈不回收，解锁时桌面一帧不重画；
HAL 在亮屏后暂停 swapd 约 6s 保护交互窗口。据此：

* 缓存进程（adj>=900）：只在压力扫描时标记。
* UI 带（100<=adj<900）：压力扫描 + 亮屏 + 亮屏后 30s 之外 + 连续遮挡 >=60s + `gpumem_mapped`>=192MB。
* 熄屏立即还原 UI 带 foreground（钉回代价付在熄屏期间）；adj 真正变化时还原；同值重写忽略。
* shrinker 回调只节流排队；屏幕状态挂 `dsi_panel_power_off/on`。
* `sepolicy.rule` 为读 `gpumem_mapped` 给 kernel 域补 `read`。

### 17.4 实机验证（r3，重启后）

* 读路径：强制扫描 UI 带，`stat_read_failed=0`，无 avc。
* 端到端：临时把门槛降到 40MB，亮屏压力扫描标记壁纸 -> KGSL 回收 17MB；
  熄屏 +3s 壁纸回到 foreground、reclaimed=0，`screen_off_restores` +1。参数已还原默认。
* 默认参数下熄屏 60s：2 次压力扫描标记 15 个缓存进程；桌面（742MB）、壁纸、blur、
  SystemUI 全部 foreground、reclaimed=0。
* 尚未做：解锁窗口 p95 帧时对照（需人工解锁）；亮屏重度使用下 `marked_ui` 触发后
  回到桌面的帧时。

### 17.5 顺带发现（未处理）

开机 172s 有一条 `WARNING at mm/slub.c:2013 new_slab`，调用栈是
`oplus_cpu_sched_sched_info` 的 `jank_hotthread_update_tick -> insert_hot_thread ->
kmem_cache_alloc`：tick 上下文里用了非法 GFP 标志分配，与 KGSL 桥无关。

## 18. 内核告警清零（2026-09-15 晚）

### 18.1 `oplus_cpu_sched_sched_info`：`mm/slub.c:2013 new_slab`

`jank_hotthread_update_tick -> insert_hot_thread -> kmem_cache_zalloc`。`hot_thread_node`
缓存建在带构造函数（`init_hot_thread_struct`）的 kmem_cache 上，SLUB 对"有 ctor 又带
`__GFP_ZERO`"直接 `WARN_ON_ONCE`。而且 ctor 只在新建 slab 页时跑、对象复用时不跑，
真正给每个节点清零的一直是 zalloc。处置：去掉 ctor（`kernel-compat/patches/oplus-sched-info-hotthread-slab-ctor.patch`）。
重编后 `__versions` 140 项与旧件逐项一致。重启后 `hot_thread_node` 已有 192 个活动对象，无告警。

### 18.2 模块加载期 11 条 `proc_dir_entry ... already registered`

补了 `oplus_bsp.log` 的加载期告警快照（原先的关键字只命中告警里的模块清单行，
头与调用栈全丢，dmesg 90 秒内就滚掉）后看清：多个一加模块各自
`proc_mkdir("oplus_mem"/"task_info")`，撞名的每一个都在 `proc_register` 里 WARN 并污染内核。
一加代码本来就有"返回 NULL 则用 `oplus_mem/<node>` 路径"的回退，节点位置从来是对的，
坏的只有告警。

| 模块 | 源码位置 | 撞名 |
|---|---|---|
| dump_tasks_mem / sigkill_diagnosis / process_reclaim / uxmem_opt | mport | `/proc/oplus_mem` |
| zram_opt（部署件来自 mport） | mport | `/proc/oplus_mem` |
| kswapd_opt / proactive_compact / gloom | `kernel-compat/` | `/proc/oplus_mem` |
| task_sched | mport | `/proc/task_info` |
| oplus_ipc（trans_ctrl 与 binder_sysfs 同模块内重复） | mport | `/proc/oplus_binder` |
| mm_osvelte（DBG 块与 `sys_memstat_init` 重复建 dmabuf/ashmem） | `kernel-compat/` | 子目录 |

处置：
* `kernel-compat/include/oplus_proc_compat.h`：先用 `filp_open("/proc/<dir>", O_DIRECTORY)`
  探测，存在就不再 `proc_mkdir`（走原有回退）。两种写法各自都会 WARN（目录在时 `proc_mkdir`、
  目录不在时路径式 `proc_create`），只能先探测。**`filp_open` 只在 r3 白名单里，这批模块从此依赖 r3。**
* `oplus_ipc` 同模块内直接复用 `d_oplus_binder`。
* osvelte 删掉 DBG 块里的重复创建；副作用是修好了 `mtrack_procs[]` 原先为 NULL、后续 mtrack 注册被静默丢弃的问题。
* mport 侧改动存为 `kernel-compat/patches/oplus-proc-shared-dirs.patch`。

### 18.3 顺带补回：zram_opt 125/125 的源码

第十二节固化的 `swappiness 125/125` 只存在于 15:11 那次 out-r2 构建产物里，源码改动从未入库
（仓库 `oplus_memory_policy` 与 mport 两份都还是 160/200）。本次从 mport 重编时补回并存为
`kernel-compat/patches/oplus-zram-opt-stock-swappiness.patch`；`.data` 为 `0x7d/0x3c/0x7d`，
与部署件一致。注意需带 `KBUILD_EXTRA_SYMBOLS=<osvelte Module.symvers>`，否则
`oplus_read_mm_config` 丢 CRC；`oplus_ipc` 同理需要 sched_assist 的 symvers。

### 18.4 验证（r3，重启后）

* 12 个重编模块 `__versions` 与旧件相比只多 `filp_open/filp_close/snprintf`，
  以及 r3 开了 `CONFIG_USER_NS` 带来的 `from_kuid`；无 CRC 变化，与 r3 `Module.symvers` 全部一致。
  其它部署件从这批模块导入的符号（hybridswap 的 3 个）CRC 全部匹配。
* 39 个 BSP 模块全部加载、0 失败；`/proc/oplus_mem`、`/proc/task_info`、`/proc/osvelte`、
  `/proc/oplus_binder`、`/proc/oplus_scheduler` 共 81 项节点清单与改动前逐项相同。
* 加载期告警快照只剩 1 条：联想原厂 `cps_wls_charger` 的
  `Attribute update_fw: read permission without 'show'`（1.7s，vendor 二进制，不在本项目源码内）。
* 运行期 dmesg：`already registered` 0 条，`WARNING` 0 条。

## 19. 锁屏亮屏卡顿：对照手机与三处修复（2026-09-16）

### 19.1 同样待机 9 分钟后并行亮屏

| | 对照机 PKX110 | 平板（修复前） |
|---|---:|---:|
| allocstall | +212 | +955 |
| 直接回收 | 49 MB | 231 MB |
| SystemUI p99 / 慢 UI 帧 | 21 ms / 6 | 81 ms / 15 |
| system_server 主线程 Slow dispatch | 无 | FaceAuthBridge 106 ms、SystemStylusHooks 211 ms |

### 19.2 两机相同的部分（更正前一轮结论）

* **swapd_pause**：两机都是 system_server 内 Athena（`athena.fg`）经 performance HAL code 0x44 置 1/清 0，窗口每次 1~6 s 不等。前一轮"手机 6 s、平板 1 s"是单次采样的误读。
* **PinnerService**：对照机同样 0 字节（同一份空的 `oplus_pinfile_config_v2.xml`），不是差异点，"钉住 SystemUI"的建议撤回。
* **水位**：对照机熄屏时 MemFree 也只有 255~325 MB，不预留余量，"熄屏抬 watermark_scale_factor"的方案不符合原厂，放弃。
* memcg 结构、zram_opt swappiness 125、THP never、kswapd_opt 的 costly-order 去回收位两边一致；相机 provider 两边都在根 memcg、无特殊保护。
* performance HAL 两机二进制并不相同（md5 不同），但都支持 swapd_pause。

### 19.3 差异与处置

1. **PenBridge Hook 主线程阻塞（本项目自身）**：`SystemStylusHooks` 的广播接收器对所有广播（含 SCREEN_ON）600 ms 后在 system_server 主 looper 上执行 `syncColorOsPenState`，亮屏时单次 211 ms，并让下一条主线程消息延迟 202 ms。亮屏/熄屏不再触发；其余同步合并后放到笔的 `LenovoPenHall` 线程。验证：亮屏后该类 Slow dispatch 消失。
2. **人脸解锁在主线程睡 100 ms**：移植包的 `ax.nd.faceunlock.FaceAuthBridge.startAuthenticate` 把启动序列 post 到主 looper，其中含 `Thread.sleep(100)`，每次亮屏主线程阻塞 103~114 ms；对照机是独立进程的原厂 face HAL（`…biometrics.face@1.0-service_uff`）。新增 BaseFix 入口 `FaceAuthOffMainThread`：原样执行同一 lambda，但放到专用线程；`stopAuthenticate` 递增代数，取消后未执行的启动直接丢弃。验证：正常亮屏无主线程 Slow dispatch；亮屏后立即熄屏，`cancelAuthentication` 后相机 1 s 内关闭；人脸正常解锁。
3. **人脸解锁白配一条 13MP JPEG 流**：控制器经 Camera1 只设 640x480 预览，picture-size 保持 4160x3120，cameraserver 的 Camera2Client 预先建 JPEG 流（`Camera2-JpegConsumer` 4160x3120、单缓冲 19.6 MB、8 个 HAL 缓冲、产出 0 帧），CamX 为其建全分辨率拍照管线，每次会话 dmabuf +363 MB（对照机 +160 MB）。同一入口在 `setupCameraParameters` 之后把 picture-size 降到预览同比例的最小尺寸（1920x1440）。验证：日志 `4160x3120 -> 1920x1440`，一次会话峰值 +254 MB（该次 1 s 即解锁成功，峰值偏保守）。
4. **lru_gen 对齐**：对照机 `init.target.rc` 写 `lru_gen/enabled 1`（0x0001），平板联想 vendor 没有，停在 0x0003（回收时额外遍历页表）。FixModule `service.sh` 在原厂 post_boot 之后写一次 1。

### 19.4 仍未解决

* 内存量级差（11.4 GB vs 7.8 GB）决定了长待机后亮屏仍会有直接回收；刚重启后的一次测量（缓存冷、应用恢复中）直接回收 ~740 MB、p99 150 ms，与重启前数据不可比，需在系统稳定后复测。
* 脚本化 `KEYCODE_SLEEP` 后约 5.7 s，`pmic_resin`（deviceId 8）上报 scanCode 12（KEYCODE_MINUS）以 WAKE_KEY 唤醒屏幕，并随之触发一次人脸相机会话。只在脚本测试中观察到，未查明，与本轮修复无关。
