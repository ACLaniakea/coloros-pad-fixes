<p align="center">
  <img src="https://raw.githubusercontent.com/ACLaniakea/coloros-pad-fixes/main/assets/readme-hero.png" alt="ColorOS Pad Fixes — 联想小新 Pad Pro GT · SM8650Q" width="100%">
</p>

# v4.0.2

v4.0.1 之上的修正发布：全部模块与配套 APK 统一为 `4.0.2 / 400002`。两处功能改动，其余模块仅为版本对齐重建。

> [!NOTE]
> 本次未变更内核。`boot` 与 `vendor_boot` 沿用 v4.0.0，**不需要重刷**。

## 相比 v4.0.1

### ZUI 相机在其他设备上闪退

有用户反馈相机一打开就闪退，崩溃日志的根因是：

```
java.lang.NullPointerException: getSupportedModes() must not be null
    at com.zui.camera.ui.mode.ModeRecyclerView.generateModeList(ModeRecyclerView.kt:241)
```

ZUI 相机按 `Build.DEVICE` 在内置 JSON 里查本机支持哪些拍摄模式，查不到就返回 null，Kotlin 空断言直接抛 NPE。相机模块的 zygisk 身份层本应把该进程的机型属性改写成 Lenovo，但它**把开发机上 `com.zui.camera` 的 UID 写死成了常量**——UID 由安装顺序决定，换台设备就对不上，身份层不生效，相机读到的仍是移植包的 `OP6547L1`。

现改为在 `postAppSpecialize` 读 `/proc/self/comm` 按进程名识别。该值由 AOSP 的 `SpecializeCommon()` 在这一步之前设好，读它不需要任何权限，因此与设备、与 zygisk 实现（Magisk / ReZygisk / Zygisk Next）都无关。注册改为 ABI v4→v3→v2 逐级回退，被拒时不再静默失效。

> 这不是 hybrid_mount 或 OverlayFS 的问题——先前的推测已被崩溃日志推翻。

### 8GB 内存档

**症状**：长时间锁屏后点亮，锁屏动态壁纸与解锁动画明显卡顿。

**测量**：息屏 120 秒后点亮的 6 秒窗口内，系统换入 712MB、直接回收 573MB——直接回收发生在申请内存的那个线程上下文里，即 UI 线程被同步阻塞。按进程拆分后，桌面、SystemUI、system_server 三者合计有 1GB 以上躺在 zram 里。

**原因**：ColorOS 的 swapd 按 `app_score` 分级，`score 100-399` 这档允许把 80% 内存压进 zram，而所有 memcg 分组默认停在 300。**已用真机 ColorOS 对照确认原厂同样如此，所以不是移植缺陷**；问题在于原厂把 `min_free_kbytes`、`avail_buffers` 都按内存容量分了档，唯独这个压缩比例不随内存变。同一策略在 11GB 机器上还剩 3.5GB 余量，在 7.4GiB 的本机就开始抖动。

**改动**：仅在 `RamSizeGB <= 8` 的设备上，把 `/apps/active` 与 `/apps/systemserver` 的 `app_score` 写 0（level 0，不压缩）。`/apps/inactive` 保持原厂 300——那里装着几十个纯缓存应用，整桶豁免等于在 8GB 机器上放弃 swap。12GB 及以上完全保持原厂行为。

同一次开机内 A/B（息屏 90 秒后唤醒）：

| 指标 | 原厂 `score=300` | 本档 `score=0` |
|---|---|---|
| 换入 `pswpin` | 58.6 MB | 17.6 MB（−70%） |
| 直接回收 `pgsteal_direct` | 79.7 MB | **0** |
| 主缺页 `pgmajfault` | 15731 | 5127（−67%） |

代价：`MemAvailable` 约 −200MB，两组用量收敛，零 LMK/OOM 击杀。

> [!WARNING]
> 这是**主动偏离原厂**，不是恢复原厂。若出现后台应用留存变差或击杀增多，第一个该回退的就是这里：把两组 `app_score` 写回 300 即可。

### 自定义键「选择应用」深色模式无卡片背景

亮色模式一直正常，深色下整片纯黑。原厂的做法不是给列表加卡片容器，而是**逐行画**：
`activity_app_picker` 里列表直接放在 FrameLayout 中，行布局也没有背景，Settings 备了
`card_list_item_{head,body,foot,full}_bg` 四件套按位置取用。原实现反过来做成了容器卡片。

改为逐行后仍不显示：分支探针确认资源名解析得到、`getDrawable()` 非空、`setBackground()`
执行，行内采样仍是 `#000000`，而同一位置强制不透明填充能正常渲染。原因是这些 shape 填充
`?attr/couiColorCard`，在此 context 下用 `Resources#getColor` 读取时深色返回不透明黑
—— ColorOS 有运行时 RRO 主题覆盖，资源表记录的 `#1affffff` 不是实际返回值。

最终改用 Settings 自己页面的取色入口 `COUIContextUtil.getAttrColor`。修复后与原厂应用管理
逐像素一致：深色行 `(26,26,26)` / 页面 `(0,0,0)`，亮色行 `(255,255,255)` / 页面 `(240,241,242)`。

### 缓存进程预算交还原厂分档

三个遗留调试属性（`persist.sys.aclaniakea.max_cached`、`persist.sys.max_cached_test`、
`persist.sys.oplus.max_cached_test`）把 AMS 的缓存进程上限锁在 **24** —— 比 AOSP 默认的 32
还低，是原厂 ColorOS 的 1/5（参考机 PKX110 为 128）。它们已写进持久化属性存储，每次开机
由 init 恢复。

后果：缓存预算腰斩后，任何掉到 cached 的进程都会被迅速清掉。实测全局搜索
`com.heytap.quicksearchbox` 只活了 116~258 秒，而同一次开机里桌面活了 41670 秒 ——
「全局搜索呼不出来」就是它在冷启动。

本版只把覆盖拿掉、不设任何值，交还原厂按内存分档：`CUR_MAX_CACHED_PROCESSES` 24 → **48**、
`CUR_MAX_EMPTY_PROCESSES` 12 → 24、`CUR_TRIM_CACHED_PROCESSES` 4 → 8。

### Scene 调度模块：陈旧锁导致整层静默停摆

调度模块用 `mkdir` 做互斥、只靠 EXIT trap 释放。进程被杀或设备在 apply 中途重启时 trap
不执行，锁目录永久留下。实机上 `apply.lock` 的时间戳与最后一次成功 apply 完全相同
（2026-08-25 23:02），此后**两周内每一次档位应用都空转 20 次后 `exit 4`，且不写任何日志** ——
增强调度这一层实际上是死的。

已改为：锁内记录持有者 pid，持有者进程已不在就按陈旧锁清掉；放弃时必须写日志；trap 覆盖
INT/TERM/HUP。同时修 `write_node` 的比较 —— 多值 sysfs 节点读回是 Tab 分隔而写入是空格，
逐字符比较把每一次成功写入都误报成 `write rejected`（实机日志里 5 条全是假的），真正的失败
反而被淹没。

### 已知问题：锁屏动态壁纸打拍（无解，非性能问题）

动态壁纸 `com.heytap.colorfulengine` 恒定以 60fps 渲染，而面板跑 144Hz。`144 ÷ 60 = 2.4`
不是整数，每帧只能落在第 2 或第 3 个 vsync 上，形成固定的 `3-2-3-2-2` 节拍。实测
SurfaceFlinger 图层呈现间隔：144Hz 下节拍 `{2:72, 3:48}`、抖动 3.95ms；强制 120Hz 下前 21 个
间隔全是 2、完全均匀。

**与性能无关** —— GPU 下限 231→578 MHz 四轮交替对照零差异。也无法用 per-package 刷新率策略
规避：ColorOS 的 `OplusRefreshRatePolicyImpl` 按置顶 Activity 的包名查表，而锁屏是 SystemUI
的窗口不是 Activity，背后 resumed 的仍是桌面。要消除只能把桌面降到 120Hz，代价大于收益。
参考机 PKX110 面板本身是 120Hz（120÷60=2 整除），原厂从未遇到这个组合。

### 修复与打包

- `exempt_persistent_memcg` 原实现只写 `/dev/memcg/apps/uid_$uid`，而本机是 per-package 布局、该路径仅存在一个空组，整段是空转的；现改为从 `/proc/PID/cgroup` 读进程实际所在的 memcg。
- 同一函数靠进程发现会漏掉开机时尚无 SystemUI 的 `/apps/active`，改为显式对齐两个原厂分层组。
- `build_all.sh` 补上把相机两个产物汇总到顶层 `releases/` 的步骤（v4.0.1 是手工拷的），并生成 `SHA256SUMS`。

## 已知问题

沿用 v4.0.1：移植包重打包 vendor 时丢了 `/vendor/bin/qms` 与 `/vendor/bin/qcc-vendor` 的 SELinux 标签，须在重打包时对 `vendor_file_contexts` 执行一次 `setfiles`。本机无本地调制解调器，功能上无损失。

## 安装或升级

请使用同一 v4.0.2 Release 的产物。内核沿用 v4.0.0，不需要重刷。

安装完 KernelSU 模块与 APK 后执行一次**完整重启**，不要仅重启 zygote。

<p align="center">
  <img src="https://raw.githubusercontent.com/ACLaniakea/coloros-pad-fixes/main/docs/donate-wechat.png" alt="微信赞助码" width="220">
</p>
