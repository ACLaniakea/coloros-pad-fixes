id=coloros_port_fix
name=联想平板 Pro GT - ColorOS 修复模块
author=ACLaniakea
version=2.12.2
versionCode=21202
description=TB710FU 上 ColorOS 移植包的适配修复：六核 perf 配置、温控曲线重标定、HybridSwap 回写额度与 swapd 唤醒下限按 8GB 重标定、调度基线、幽灵热区停用，以及杜比/环境光/AON/语音唤醒/Tango 修复。2.8.x 改法转向原厂源头：post-fs-data 阶段 patch 并 bind 原厂 init.oplus.nandswap.sh，service.sh 里的抢写降级为核对+回落。2.8.1 修 nosuid_transition 致命回归。2.9.0 补原厂 memcg 缺口：Nirvana 只给前台 app 写 app_score=0，system_server/SystemUI 永远轮不上、永久停在 300 被按 80% 压；boot_completed 后一次性给 adj<=-700 的常驻 uid 写 0（注：2.9.0 描述里"主缺页降到 5.6/s"是取样窗口偶然导致的误判，重启后稳态复测未复现，缺口本身真实、修法保留）。2.10.0 放行 binder UX 通路：system_server 的 android.anim/android.display 对 /proc/oplus_binder/ux_flag 的 ioctl 一直被 SELinux 挡着（90 秒滑动新增 9 条 avc denied），binder UX 优先级继承整条不生效，本版补上 sepolicy；另压掉基带残留造成的 ServiceManagerCppClient 刷屏（占安静期日志量约四成）。2.11.0 接上 KGSL 显存回收：移植包里没有任何组件写 /sys/class/kgsl/kgsl/proc/<pid>/state，所有进程恒为 foreground（含 adj=965 的已缓存进程），高通自带的 GPU 显存回收从未运行过一次，8GB 机器上白压 600MB~1.4GB 不可回收显存；新增 bin/kgsl-state-sync.sh 按 oom_score_adj 双向同步状态（阈值 800，刻意排除桌面 600 与上一个应用 700），实测手动标记 4 个后台进程 15 秒回收 146MB。这是本模块少数常驻之一，理由是原厂根本没有这个写入者、内核也不会自动写回 foreground，而 Android 侧无可用事件源。 2.11.1 撤掉音频断流的元凶：2.11.0 里把 OPPO 手机那份 PAL 配置（CUSTOM_VOICE_UI / DUAL_MIC / vendor_uuid=68ac2d41-…）bind 到/vendor/etc/audio/sku_pineapple/resourcemanager_pineapple_mtp.xml，方向是反的——两家 ADSP 各带各的自定义唤醒模块：平板(联想固件)是 capi_aispeech_wakeup，手机(OPPO 固件)是 capi_breeno_ecns。让 aispeech 模块去吃 breeno 的图，ADSP 每 1.2 秒崩一次（EX:audio_process:0x2:GC_E00001D1），用户侧就是播放每隔几秒断一下。本版取消该 bind，PAL 配置保持联想原样（dc90a99a + aispeech + SINGLE_MIC），并把 force-BWV 标记改回开机时立起。顺带纠正 2.11.0 注释里的两个错判：dc90a99a 不是移植包留的占位，它出现在联想原厂 vendor dump 同一文件里，是联想自己唤醒词的 uuid；另外 LOAD_PHRASE_MODEL 后面那个数字是 model handle 不是状态码，当时据此判定的「DSP 已跑通」不成立。 2.11.2 修内存扩展改成 8GB 重启后仍显示 6GB：原厂 init.oplus.nandswap.sh 的档位链只认 4/8/12，本机 persist.sys.oplus.nandswap.cfg 却是 "4,6,8"；而且 $prop_nandswap_size 是脚本第 13 行加载时就 getprop 好的，真正干活的只有开机 154ms 那次 post-fs-data 调用（第 123 行 sys.oplus.nandswap.init 一次性闸门，boot_completed 那次直接 exit）。结果无论选几 GB 都落进兜底分支，disksize 恒为 4096+2048=6144M，/proc/swaps 恒显示 6GB；而 eswap 那半边读的是 swapsize.curr，确实按 8GB 走了，所以只有 zram 半边掉队。本版在已有的 patch_stock_nandswap 里加第三条 sed：档位改为 2~16GB 通用换算 size*1024，swapsize 读空时回落到 swapsize.curr；zram_increase_limit 保持原厂的 0，disksize 正好等于用户选的容量。同时撤掉 2.11.1 那段 OVMS uuid 的 mount --bind：bind 本身成功，但 KernelSU 随后在整个 /my_product/etc 目录上盖 overlay，把文件级 bind 整个遮住；正确投递方式是直接改模块自带的 my_product/etc/OVMS_settings.xml 覆盖文件（KSU 本来就拿它当 lowerdir）。lenovo_uuid_enable 开关与 service.sh 里的开机安全网一并作废，force-BWV 恢复为无条件立起。 2.12.0 纠正 2.11.2 的两处错判并接上调度基线：(1) 2.11.2 说"正确投递方式是改模块自带的 my_product/etc/OVMS_settings.xml，KSU 本来就拿它当 lowerdir"——**这是错的**，KernelSU 对所有普通应用进程卸载模块挂载，应用读到的永远是 /my_product 分区原件（system_server 有 overlay，OVMS/设置 都没有），实测从 OVMS 进程视角回读仍是旧值；(2) /data/local/tmp/ovoice_force_bwv 这个 force-BWV 文件开关**从来没生效过**，应用域读不到 shell_data_file，File.exists() 恒为 false，于是一直跑的是"DSP 优先、失败降级"，白搭 9 秒探测窗口。两件事都改到 Hook 侧解决（见 base-fix v1.1.16）。另外 DSP 一阶段已实测判死：只换 vendor_uuid、保留联想全部图和 SINGLE_MIC profile，PAL 全线打通、LoadSoundModel status 0，但联想 ADSP 的 capi_aispeech_wakeup 解不了小布的 BreenoSpeech 模型，20 秒崩 15 次，故 Hook 侧默认永久停用 DSP。
