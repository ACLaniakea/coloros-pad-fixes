id=aclaniakea_oplus_bsp
name=OPlus BSP Kernel Modules (TB710FU)
version=v3.0.0
versionCode=300000
author=ACLaniakea
description=按依赖顺序加载 30 个一加 BSP 内核模块：调度增强、压缩内存、后台冻结。必须先刷本项目的自建内核（boot + vendor_boot 两个分区），否则不要装。
