# 已归档：合并前的独立基础修复模块

**不要安装这个目录。** 它是 2.0.0 合并之前 `coloros_port_base_fix` 的快照，
留着只为读源码时对照。现在生效的是 `fix-module/`（模块 id `coloros_port_fix`，
发布名 `FixModule`），基础修复与调优两部分都在里面。

构建脚本 `tools/build_all.sh` 不会打包这个目录。
